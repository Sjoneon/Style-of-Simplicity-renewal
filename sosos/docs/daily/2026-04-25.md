# Daily Log - 2026-04-25

## 메타 정보
- 프로젝트: SOS 리뉴얼
- 우선순위: AI 챗봇 MVP
- 오늘 목표: 우측 하단 AI 버튼 + 의류 추천 전용 챗봇 API 구현
- 완료 기준(DoD): Gemini 키 설정 시 서버를 통해 스타일 추천 응답이 나오고, 의류 외/부적절 요청은 선차단

## 의사결정 1줄
- Gemini 키는 프론트가 아니라 백엔드 환경변수로만 처리한다. 이유: 키 누출 방지와 운영 통제 때문이다.

## 사전점검 체크리스트
- [x] 구현 전 학습 문서 작성 (`무엇/왜/언제/어떻게`)
- [x] 작업 파일 경로/완료 기준 사전 명시
- [x] 의류 도메인 제한/외부 호출 비용 절감 정책 선설계

## 오늘 작업
1. 프론트 챗봇 UI 추가
- 신규: `frontend/src/components/AiStylistWidget.jsx`
- 적용: `frontend/src/components/AppLayout.jsx`
- 신규: `frontend/src/services/aiStylistApi.js`
- 내용
  - 우측 하단 원형 버튼(`1.3cm x 1.3cm`, 텍스트 `AI`)
  - 버튼 클릭 시 소형 채팅창 열기/닫기
  - 채팅 응답에 추천 상품 카드 노출 + 상품 상세 이동

2. 백엔드 AI 추천 API 추가
- 신규: `src/main/java/com/prosos/sosos/controller/api/v1/AiStylistApiController.java`
- 신규: `src/main/java/com/prosos/sosos/service/AiStylistService.java`
- 신규 DTO
  - `AiStylistChatRequest`
  - `AiStylistChatResponse`
- 내용
  - `POST /api/v1/ai/stylist/chat`
  - 의류 외 질문 차단, 부적절 요청 차단
  - 입력 길이 제한(기본 180자)
  - 판매자 등록 키워드/상품 기준 추천 후보 산출
  - Gemini 키 존재 시 모델 호출, 미설정/오류 시 로컬 fallback 응답
  - 키 전달은 URL 쿼리 대신 `x-goog-api-key` 헤더 사용

3. 추천 후보 조회 보강
- 수정: `src/main/java/com/prosos/sosos/repository/ProductRepository.java`
- 내용: 재고 있는 상품 + 키워드 fetch 조회 메서드 추가

4. 환경변수/보안 설정
- 수정: `src/main/resources/application-dev.properties`
- 수정: `src/main/resources/application-prod.properties`
- 신규: `sosos/.env.example`
- 내용
  - `GEMINI_API_KEY`, `GEMINI_MODEL`, `GEMINI_BASE_URL` 등 추가
  - 키는 백엔드 env만 사용, 프론트 미노출

## 검증 결과
- 백엔드 컴파일: 성공
  - `cd sosos && .\mvnw.cmd -DskipTests compile`
- 프론트 린트: 성공
  - `cd frontend && npm run lint`
- 프론트 빌드: 성공
  - `cd frontend && npm run build`
  - 번들 경고(500KB+)는 기존과 동일

## 다음 작업
1. 챗봇 응답 품질 개선용 프롬프트/키워드 사전 튜닝
2. 사용자 요청 rate limit(세션 기준) 추가
3. Gemini 호출/차단 로그 운영 지표 정리

## 추가 작업 - AI 로그인 필수화 (2026-04-25)
- AI 추천 API(`/api/v1/ai/stylist/chat`)에 세션 로그인 검증 추가
- 비로그인 상태에서는 AI 위젯 입력/전송 비활성화 + 로그인 이동 버튼 노출
- 홈 화면 비로그인 안내 문구(`로그인하면 검색 기록이 저장되고...`) 제거
- 검증: backend compile, frontend lint, frontend build 성공
- 피드백 반영: AI 사용 대상 범위를 user 전용에서 로그인 계정(user/seller) 공통으로 수정
- repository 주석 정리: 메서드별 목적 주석 통일(15개 파일)
