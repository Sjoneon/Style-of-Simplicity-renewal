# Daily Log - 2026-04-27

## 메타 정보
- 프로젝트: SOS 리뉴얼
- 우선순위: P2/P3 전환 준비
- 오늘 목표: 배포 준비 파일 정리 + S3 실검증 경로 고정 + EC2 IAM Role 리허설 절차 확정
- 완료 기준(DoD): 환경변수 누락 시 즉시 실패, CORS 도메인 외부화, Dockerfile/환경변수 표준 문서/리허설 문서 반영, 검증 명령 실행

## 의사결정 1줄
- `.env 자동 주입 코드`는 적용하지 않고, 필수 환경변수 누락을 시작 단계에서 즉시 차단하는 방식으로 정리한다. 이유: 사용자 피드백 반영 + 실행 원인 명확화.

## 사전점검 체크리스트
- [x] 구현 전 학습 문서 작성 (`무엇/왜/언제/어떻게`)
- [x] 작업 파일 경로/완료 기준 사전 명시
- [x] 프론트/백엔드/스모크 테스트 검증 계획 수립

## 오늘 작업
1. 백엔드 시작 실패 조건 명확화
- 수정: `src/main/java/com/prosos/sosos/SososApplication.java`
- 내용
  - `DB_USERNAME`, `DB_PASSWORD` 누락 시 애플리케이션 시작 전 즉시 실패 처리
  - `prod + s3` 조합에서 `APP_STORAGE_S3_BUCKET` 누락 시 즉시 실패 처리

2. CORS 도메인 외부화
- 수정: `src/main/java/com/prosos/sosos/config/WebConfig.java`
- 수정: `src/main/resources/application-dev.properties`
- 수정: `src/main/resources/application-prod.properties`
- 내용
  - 하드코딩 도메인 제거
  - `APP_CORS_ALLOWED_ORIGINS`(쉼표 구분) 기반으로 허용 도메인 로드
  - 값이 비어 있으면 로컬 기본 도메인으로 fallback

3. 배포 준비 파일 추가
- 신규: `Dockerfile`
- 신규: `.dockerignore`
- 신규: `docs/deployment/env-standard.md`
- 신규: `docs/deployment/ec2-iam-role-rehearsal.md`
- 수정: `.env.example`
- 내용
  - 멀티스테이지 Docker 빌드 기준 추가
  - 운영 주입 환경변수 키 표준화
  - EC2 + IAM Role 리허설 절차(메타데이터 확인, 스모크 테스트, 실패 점검 순서) 문서화

4. S3 실검증 자동화 경로 정리
- 신규: `src/test/java/com/prosos/sosos/service/storage/S3CredentialSmokeTest.java`
- 내용
  - `RUN_S3_SMOKE_TEST=true`일 때만 실행되는 실자격증명 업로드/조회/삭제 스모크 테스트 추가
  - 기본 자격증명 체인(`DefaultCredentialsProvider`) 사용

5. 학습/인덱스 문서 동기화
- 수정: `docs/daily/learning/2026-04-27-deploy-prep-and-env-guard-learning.md`
- 수정: `docs/daily/learning/README.md`

## 검증 결과
- 백엔드 컴파일: 성공
  - `cd sosos && .\mvnw.cmd -DskipTests compile`
- 백엔드 실행 가드: 성공
  - `DB_USERNAME`, `DB_PASSWORD`를 비우고 실행 시 즉시 실패 메시지 확인
  - 확인 메시지: `Missing required environment variable: DB_USERNAME`
- 프론트 린트: 성공
  - `cd frontend && npm run lint`
- 프론트 빌드: 성공
  - `cd frontend && npm run build`
- S3 스모크 테스트(기본 실행): 환경 준비 누락으로 Skip
  - 실행 명령: `cd sosos && .\mvnw.cmd -Dtest=S3CredentialSmokeTest test`
  - Skip 원인: `RUN_S3_SMOKE_TEST` 또는 `APP_STORAGE_S3_BUCKET` 미설정
- S3 스모크 테스트(강제 실행): 자격증명 누락으로 실패
  - 실행 명령: `RUN_S3_SMOKE_TEST=true` + 더미 버킷 설정 후 `.\mvnw.cmd -Dtest=S3CredentialSmokeTest test`
  - 실패 원인: `Unable to load credentials from any of the providers...`

## 이슈/제한사항
- 현재 작업 환경에는 `aws` CLI 명령이 없음
- 로컬/기본 자격증명 체인에 AWS 실자격증명이 없어 실업로드 성공까지는 이번 실행에서 확정 불가

## 다음 작업
1. 실버킷 값 세팅 후 `RUN_S3_SMOKE_TEST=true`로 테스트 1회 성공 로그 확보
2. EC2 인스턴스에서 `docs/deployment/ec2-iam-role-rehearsal.md` 절차 그대로 리허설 수행
3. 배포 도메인 최종값으로 `APP_CORS_ALLOWED_ORIGINS` 운영값 확정

## 추가 작업 (테스트 상품 대량 세팅)
1. 학습 문서 선작성
- `docs/daily/learning/2026-04-27-test-product-seeding-learning.md`

2. 자동 등록 스크립트 추가
- 신규: `scripts/seed-test-products.ps1`
- 동작
  - 기본 백엔드: `http://localhost:8085`
  - 기존 상품의 `sellerId` 자동 감지
  - `images/` 디렉터리 파일을 순환 사용해 테스트 상품 등록
  - 등록 결과(생성/실패/최종 개수) 출력

3. 실행 결과
- 실행 명령: `./scripts/seed-test-products.ps1 -Count 20`
- 결과
  - 초기 상품 수: 3
  - 신규 생성: 20
  - 실패: 0
  - 최종 상품 수: 23

4. 트러블슈팅
- 초기 실패 원인
  - `/api/v1/products` 등록 시 `keywords` JSON multipart 인코딩 이슈
  - 스크립트에서 `sellerId`를 nullable로 다뤄 전송값이 비는 문제
- 해결
  - 등록 경로를 `/seller/products/product_register`로 전환
  - 스크립트 파라미터를 `SellerId` 기본값 0 + 자동 감지 로직으로 수정

5. 접속 주소 재확인
- 소비자(React): `http://localhost:3000/`
- 판매자(React 로그인): `http://localhost:3000/admin/login`
- 소비자(레거시): `http://localhost:8085/`
- 판매자(레거시): `http://localhost:8085/seller/login`

## 추가 작업 (AI 추천 의도 반영 보강)
1. 사용자 피드백
- 문제: `상의/외투/바지` 요청에도 `Bag`, `Loafer` 추천이 섞여 의도 불일치 발생

2. 로직 개선
- 수정: `src/main/java/com/prosos/sosos/service/AiStylistService.java`
- 내용
  - 사용자 메시지에서 요청 카테고리(`TOP/BOTTOM/OUTER/SHOES/BAG/ACC`) 추론
  - 의류 중심 요청 시 비의류(가방/신발/액세서리) 후보를 점수 계산 전에 제외
  - 카테고리 불일치 상품 제외 규칙 추가
  - 상세 조건을 이미 입력한 경우 안내 문구 반복 방지

3. 테스트 추가
- 신규: `src/test/java/com/prosos/sosos/service/AiStylistServiceTest.java`
- 검증 항목
  - 의류 요청에서 bag/shoes 제외
  - 상세 조건 입력 시 반복 안내 문구 미노출
  - 일반 요청에서 상세 조건 요청 문구 유지

## 추가 작업 (AI 추천 혼합 카테고리 의도 보강)
1. 사용자 피드백(추가)
- 요청: 의류-only가 아닌, `비의류-only`와 `의류+비의류 혼합` 요청도 정확히 반영 필요

2. 로직 보강
- 수정: `src/main/java/com/prosos/sosos/service/AiStylistService.java`
- 내용
  - 상품명 기반 유효 카테고리 보정(`BAG/SHOES/ACC`)을 점수/필터/출력에 공통 적용
  - 사용자가 카테고리를 명시한 경우에는 기본 fallback 점수(`기본 패션 추천`)를 부여하지 않도록 조정
  - 결과적으로 명시 의도와 무관한 후보(특히 카테고리 미정/오분류 데이터) 유입 가능성 축소

3. 테스트 보강
- 수정: `src/test/java/com/prosos/sosos/service/AiStylistServiceTest.java`
- 검증 항목 추가
  - `가방만` 요청 시 가방 카테고리만 추천되는지 검증(카테고리 미정 상품 누수 차단 포함)
  - `상의+가방` 혼합 요청 시 TOP/BAG만 남고 BOTTOM/SHOES가 제외되는지 검증

4. 검증 결과
- 백엔드 컴파일: 성공
  - `cd sosos && .\mvnw.cmd -DskipTests compile`
- 단위 테스트: 성공
  - `cd sosos && .\mvnw.cmd -Dtest=AiStylistServiceTest test`
  - 결과: `Tests run: 5, Failures: 0, Errors: 0`
