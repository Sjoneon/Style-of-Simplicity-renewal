# Daily Log - 2026-02-24

## 메타 정보
- 프로젝트: SOS 쇼핑몰 리뉴얼
- 오늘 우선순위: P2
- 오늘 목표: `frontend/` 생성 + React/Vite/MUI/Axios 초기화 + `/api/v1/products` 연동 코드 작성
- 완료 기준(DoD): 프론트 로컬 실행 확인, 백엔드 API 1개 연동 코드 확인

## 개발 사전점검 체크리스트
- [x] 구현 전에 학습용 설명 파일을 먼저 작성했다.
- [x] 설명 순서를 `무엇/왜/언제/어떻게`로 작성했다.
- [x] 어려운 용어에 쉬운 설명을 붙였다.
- [x] 오늘 작업 대상 파일 경로를 먼저 정리했다.
- [x] 완료 기준(Definition of Done)을 1줄로 명시했다.

## 구현 전 학습 파일
- `sosos/docs/daily/learning/2026-02-24-frontend-bootstrap-learning.md`

## 오늘 작업 내용
1. 프론트 초기 프로젝트 생성
- `frontend/` 생성 (Vite React 템플릿)
- 의존성 설치: `@mui/material`, `@emotion/react`, `@emotion/styled`, `axios`

2. API 연동 구조 작성
- `frontend/src/services/api.js` Axios 인스턴스 생성
- `frontend/src/services/productApi.js` `GET /api/v1/products` 호출 함수 추가
- `frontend/src/components/ProductList.jsx` 목록/로딩/에러 UI 분리
- `frontend/src/App.jsx` API 호출 및 상태 관리
- `frontend/src/main.jsx` MUI ThemeProvider + CssBaseline 적용
- `frontend/src/index.css` 기본 스타일 정리
- `frontend/vite.config.js` 개발 서버 포트 `3000`, `/api` 프록시(`http://localhost:8085`) 설정

3. 백엔드 연동 준비
- `sosos/src/main/java/com/prosos/sosos/config/WebConfig.java`
- `/api/**` CORS 허용 Origin에 `localhost:3000`, `localhost:5173` 추가

## 검증 결과
- 프론트 빌드: 성공
  - 명령어: `cd frontend && npm run build`
- 프론트 개발 서버: 포트 3000 리슨 확인
  - 명령어: `cd frontend && npm run dev -- --host 127.0.0.1 --port 3000`
- 프론트 린트: 성공
  - 명령어: `cd frontend && npm run lint`
- 백엔드 기동: 성공
  - 명령어: `cd sosos && ./mvnw.cmd spring-boot:start`
- API 연동 검증: 성공
  - 명령어: `GET http://127.0.0.1:8085/api/v1/products`
  - 결과: `200 OK` (`{"success":true,"data":[],"message":"상품 목록 조회 성공"}`)

## 완료 판정
- 로드맵 P2-4 완료 기준(프론트 로컬 실행 + 백엔드 API 1개 연동 확인) 충족
- 공식 완료일은 `2026-02-24`로 기록
