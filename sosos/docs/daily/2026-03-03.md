# Daily Log - 2026-03-03

## 메타 정보
- 프로젝트: SOS 쇼핑몰 리뉴얼
- 오늘 우선순위: `P2`
- 오늘 목표: React 핵심 5화면 마이그레이션 + 조회->장바구니->주문 흐름 구현
- 목표 완료 시점(ETA): `2026-03-03 23:30`
- 완료 기준(DoD): React 화면에서 로그인 사용자 기준으로 조회->장바구니->주문이 실제 API 호출로 동작

## 의사결정 1줄
- 무엇/왜: `react-router-dom`을 도입해 URL 기반 화면 분리를 적용한다. 핵심 5화면 전환을 한 번에 안정적으로 관리하기 쉽기 때문이다.

## Codex 사전점검 체크리스트
- [x] 구현 전에 학습용 설명 파일을 먼저 작성했다.
- [x] 설명 순서를 `무엇/왜/언제/어떻게`로 작성했다.
- [x] 어려운 용어에 쉬운 설명을 붙였다.
- [x] 오늘 작업 대상 파일 경로를 먼저 정리했다.
- [x] 완료 기준(Definition of Done)을 1줄로 명시했다.

## 구현 전 학습 파일
- `sosos/docs/daily/learning/2026-03-03-core-screen-migration-learning.md`

## 오늘 완료한 작업
1. 백엔드 `/api/v1` 주문 경계 보강
- `POST /api/v1/orders/cart/purchase` 추가
- 세션 사용자(`User`) 검증 후 `userService.purchaseCart(user)` 호출
- 실패 시 401/400 응답 분기 처리

2. 프론트 핵심 5화면 마이그레이션
- 라우팅 구성
  - `/` 메인 목록
  - `/products/:productId` 상품 상세
  - `/cart` 장바구니
  - `/auth` 로그인/회원가입
  - `/seller/dashboard` 판매자 대시보드
- 인증 컨텍스트(`AuthProvider`) 추가
  - 로그인/로그아웃/세션 복구
  - 사용자/판매자 권한 분기
- API 모듈 분리
  - `productApi`, `userApi`, `orderApi`
- 공통 Axios 설정 보강
  - `withCredentials: true`
  - API 응답 형식 검증 + 공통 에러 메시지 유틸

3. 핵심 사용자 흐름 연결
- 메인 목록에서 상품 상세 이동
- 메인/상세에서 장바구니 담기
- 장바구니에서 삭제/총액 확인/장바구니 주문
- 장바구니 화면에서 내 주문 내역 조회

4. 판매자 대시보드 연결
- 내 상품 목록(판매자 ID 기준 필터)
- 판매 주문 목록
- 주문 상태 변경(처리/취소/반품/교환)

## 검증 결과
- 프론트 린트: 성공
  - 명령어: `cd frontend && npm run lint`
- 프론트 빌드: 성공
  - 명령어: `cd frontend && npm run build`
  - 참고: 번들 크기 경고(500KB 초과) 1건
- 백엔드 테스트: 성공
  - 명령어: `cd sosos && .\mvnw.cmd test`
  - 결과: 6 tests, 0 failures

## 우선순위/ETA 점검
- P2 진행률: 핵심 화면 1차 마이그레이션 완료
- ETA 변동 여부: 없음

## 남은 확인 항목
- 실제 계정(사용자/판매자)으로 브라우저 수동 스모크 테스트
- 번들 크기 경고 해소를 위한 코드 분할 검토

## 피드백 반영 (회원가입 오류)
- 이슈: 회원가입 실패 시 휴대폰 형식 에러 문구가 깨져 보였고, `01062761649`처럼 하이픈 없는 입력이 바로 실패함
- 원인:
  - 백엔드 `UserService.registerUser` 검증이 `010-XXXX-XXXX`만 허용
  - 해당 예외 문구 자체가 깨진 문자열로 저장되어 프론트 Alert에 그대로 노출
- 조치:
  1. 백엔드에서 휴대폰 번호 정규화 함수(`normalizePhoneNumber`) 추가
     - 숫자만 입력(`01062761649`) -> 자동으로 `010-6276-1649` 변환
  2. 백엔드 예외 메시지 한글 정상 문자열로 교체
  3. 프론트 회원가입 제출 전 휴대폰 번호 정규화/사전검증 추가
     - 잘못된 값은 API 호출 전에 명확한 한글 메시지 표시

## 추가 UX 보강 (메인 목록 1안 + 부드러운 로그인 유도)
- 구현 전 학습 파일:
  - `sosos/docs/daily/learning/2026-03-03-home-ux-baseline-plus-learning.md`
- 작업 내용:
  1. 메인 목록 UX 1안 적용
     - 상단 검색/정렬/재고 필터 강화
     - 좌측 카테고리 스티키 내비게이션 추가
     - 상품 카드 정보 위계/액션 버튼 개선
  2. 비로그인 담기/주문 유도 개선
     - 즉시 로그인 페이지로 보내지 않고 안내 다이얼로그 표시
     - `계속 둘러보기` / `로그인하기` 선택 제공
  3. 공통 컴포넌트화
     - `LoginPromptDialog` 추가 후 홈/상품상세 재사용

## 추가 피드백 반영 (UI 베이스 복원)
- 이슈:
  - 상단 사용자명에 `(사용자)` 표기가 불필요
  - 메인 카테고리가 데이터 의존으로 줄어들어 기존 베이스(고정 카테고리) 느낌이 약해짐
- 조치:
  1. 상단 사용자 표시를 `이름만` 노출하도록 수정
  2. 메인 카테고리를 고정 목록으로 복원
     - `ALL / BEST / OUTER / TOP / BOTTOMS / SET CLOTHES / SHOES / BAG_ACC`
  3. 카테고리 필터 비교 시 공백/언더스코어 차이를 정규화해 매칭

## 추가 피드백 반영 (블랙/화이트 + 상단 카테고리)
- 이슈:
  - 블랙/화이트 기반의 더 모던한 톤 요청
  - 좌측 카테고리 대신 상단에서 카테고리를 고르는 구조 요청
  - 20-30대 패션 입문자용 탐색 탭 구성 요청
- 조치:
  1. 테마를 블랙/화이트 중심으로 재구성
     - `primary=#111111`, 배경/보더 그레이 스케일 통일
  2. 메인 구조를 상단 선택형으로 변경
     - 좌측 카테고리 제거
     - 상단 `탐색 탭` + `카테고리 탭` 2단 구조 적용
  3. 입문자용 탐색 탭 제안/적용
     - `처음 시작 / 선물 추천 / 신상 / 기본템 / 출근 룩 / 랭킹`
     - 각 탭은 가격대/카테고리/정렬 규칙으로 동작하도록 연결

## 추가 피드백 반영 (상단 메뉴/배너/검색 진입)
- 이슈:
  - 상단 `메인 목록` 버튼과 사용자 이름 노출이 불필요
  - 소개 문구 고정 배너가 아닌 판매자 업로드 기반 추천 배너 필요
  - 검색 영역이 화면을 과도하게 차지
  - 탐색 탭과 카테고리 탭 사이 간격이 커서 시선이 분산
- 조치:
  1. 상단 메뉴 재구성
     - `고객센터 / 마이페이지 / 알림 / 로그인·로그아웃`
     - 사용자 이름 칩 제거
  2. 홈 상단 배너를 상품 이미지 캐러셀로 변경
     - 이미지 있는 상품을 자동 순환 노출
     - 배너 클릭 시 해당 상품 상세 이동
  3. 검색 영역을 돋보기 아이콘 진입형으로 변경
     - 클릭 시 검색 다이얼로그 오픈
     - 최근 검색어(로그인 사용자별 로컬 저장) + 인기 검색어(로컬 통계) 노출
  4. 탭 배치 정리
     - 탐색 탭: `처음 시작 / 선물 / 신상 / 기본템 / 출근 룩 / 랭킹`
     - 카테고리 탭을 바로 아래 붙여 배치
  5. 고객센터/마이페이지/알림 라우트 추가

## 추가 피드백 반영 (검색 위치/결과 카운트 단순화)
- 구현 전 학습 파일:
  - `sosos/docs/daily/learning/2026-03-03-header-search-result-count-update-learning.md`
- 이슈:
  - `탐색 탭: ... / 카테고리: ...` 안내 줄은 정보 과밀
  - 검색은 상단 우측 빠른 접근이 더 적합
  - `검색 결과 0개` 문구보다 `0개`처럼 단순 표기가 선호됨
- 조치:
  1. 헤더를 2단으로 구성
     - 1단: `고객센터 / 마이페이지 / 알림 / 로그인·로그아웃`
     - 2단: 우측 `검색 / 장바구니` 버튼
  2. 홈에서 탐색/카테고리 안내 텍스트 줄 제거
  3. 헤더 검색 버튼 클릭 시 `/?openSearch=1`로 진입해 검색 다이얼로그 자동 오픈
  4. 검색 결과 개수 영역은 검색어 적용 시에만 노출
     - 표기: `N개`

## 추가 피드백 반영 (검색-정렬 단계 분리 + 기록 저장 확인)
- 구현 전 학습 파일:
  - `sosos/docs/daily/learning/2026-03-03-search-sort-flow-update-learning.md`
- 이슈:
  - 검색 팝업에서 정렬까지 동시에 고르는 흐름이 복잡함
  - 검색 기록/인기 검색어 저장이 사용자 기준으로 남는지 확인 필요
- 조치:
  1. 검색 팝업에서 정렬 옵션 제거
     - 검색어 + 재고 필터 중심으로 단순화
  2. 정렬 옵션을 검색 후 결과 박스로 이동
     - 결과 개수(`N개`)와 함께 같은 영역에서 변경
  3. 저장 로직 검증
     - 최근 검색어: `sos-search-history-user-{id}` 키(로그인 사용자별)
     - 인기 검색어: `sos-popular-search-terms-v1` 키(브라우저 단위 누적)

## 추가 피드백 반영 (소규모 운영용 슈퍼관리자 단일 권한)
- 구현 전 학습 파일:
  - `sosos/docs/daily/learning/2026-03-03-super-admin-only-access-learning.md`
- 이슈:
  - 사용자 로그인과 관리자 로그인이 같은 진입점이라 운영 동선이 혼재됨
  - 소규모 테스트 운영에서는 관리자 권한을 단일 슈퍼관리자로 단순화할 필요가 있음
- 조치:
  1. 관리자 전용 로그인 페이지 추가
     - 경로: `/admin/login`
  2. 관리자 전용 라우트 가드 추가
     - 경로: `/admin/dashboard`
     - 접근 조건: `userType === 'seller'`
  3. 기존 판매자 대시보드 경로 호환 처리
     - `/seller/dashboard` -> `/admin/dashboard` 리다이렉트
  4. 일반 로그인 화면 판매자 이동 경로 변경
     - 판매자 로그인 성공 시 `/admin/dashboard`로 이동
  5. 대시보드 문구를 슈퍼관리자 기준으로 정리

## 추가 작업 (테스트용 슈퍼관리자 계정 생성)
- 구현 전 학습 파일:
  - `sosos/docs/daily/learning/2026-03-03-create-test-super-admin-learning.md`
- 조치:
  1. 로컬 DB `sos_db.sellers`에 테스트 슈퍼관리자 계정 upsert
  2. 생성 결과 조회로 계정 존재 확인
- 생성 계정(로컬 테스트 전용):
  - 사업자번호: `111-11-11111`
  - 비밀번호: `1234`

## 추가 피드백 반영 (관리자 전용 대시보드 진입 버튼)
- 구현 전 학습 파일:
  - `sosos/docs/daily/learning/2026-03-03-admin-dashboard-entry-button-learning.md`
- 이슈:
  - 슈퍼관리자가 메인으로 이동한 뒤 대시보드 재진입 경로가 부족함
  - 비로그인/일반 사용자에게 관리자 진입 버튼 노출은 금지되어야 함
- 조치:
  1. 헤더 네비게이션에 `슈퍼관리자 대시보드` 버튼 추가
  2. 버튼 노출 조건: `userType === 'seller'`
  3. 비로그인/일반 사용자에서는 버튼 미노출 유지
  4. 기존 관리자 보호 라우트(`RequireAdmin`)는 그대로 유지

## 추가 피드백 반영 (일반 로그인/관리자 로그인 안내 분리)
- 구현 전 학습 파일:
  - `sosos/docs/daily/learning/2026-03-03-auth-separation-user-admin-learning.md`
- 이슈:
  - `/auth` 문구에 판매자 로그인 안내가 남아 관리자 전용 로그인 구조와 충돌
- 조치:
  1. `/auth` 로그인 설명을 사용자 이메일 전용 문구로 변경
  2. 로그인 입력 라벨/placeholder를 이메일 전용으로 변경
  3. 로그인 폼 하단에 `/admin/login` 이동 버튼(`슈퍼관리자 로그인`) 추가
  4. `/auth`에서 관리자 계정 로그인 시도 시 세션 종료 후 `/admin/login`으로 유도

## 추가 피드백 반영 (슈퍼관리자 운영 기능 확장)
- 구현 전 학습 파일:
  - `sosos/docs/daily/learning/2026-03-03-super-admin-operations-dashboard-learning.md`
- 이슈:
  - 기존 대시보드는 조회 중심이라 실제 운영 업무(상품/주문/문의 처리)에 부족함
- 조치:
  1. 관리자 상품 API 클라이언트 확장
     - `frontend/src/services/productApi.js`
     - 상품 등록/수정/삭제(FormData) 함수 추가
  2. 문의 관리 API 클라이언트 추가
     - `frontend/src/services/inquiryApi.js`
     - 문의 목록 조회, 답변 등록/수정, 문의 삭제
  3. 슈퍼관리자 대시보드 탭형 재구성
     - `frontend/src/pages/SellerDashboardPage.jsx`
     - 탭: `운영 요약 / 상품 관리 / 주문·배송 / Q&A`
  4. 운영 요약 지표 추가
     - 등록 상품 수, 누적 매출, 오늘 주문, 미답변 Q&A, 상태별 주문 집계
  5. 실무 액션 연결
     - 상품 등록/수정/삭제
     - 주문 상태 변경(처리/취소/반품/교환)
     - 사용자 문의 답변 등록/수정 및 삭제

## 추가 피드백 반영 (상품 등록 오류 수정)
- 구현 전 학습 파일:
  - `sosos/docs/daily/learning/2026-03-03-product-upload-path-fix-learning.md`
- 이슈:
  - 슈퍼관리자 상품 등록 시 이미지 저장 단계에서 실패
  - 원인 메시지가 깨져 화면에서 원인 파악이 어려움
- 원인:
  - 업로드 경로가 `C:/Users/Roneon/Desktop/SosProject/images`로 하드코딩되어 현재 환경에서 권한 거부 발생
- 조치:
  1. `SellerService` 업로드 경로를 설정 기반(`app.upload.base-dir`)으로 전환
  2. `WebConfig`의 `/images/**` 리소스 매핑도 같은 설정 경로 기준으로 변경
  3. `ProductApiController` 상품 등록/수정 응답 메시지 한글 정상화 + Runtime 예외 응답 처리
  4. `application-dev.properties`, `application-prod.properties`에 `app.upload.base-dir=${APP_UPLOAD_BASE_DIR:uploads}` 추가
  5. 변경 후 `mvnw clean test`로 전체 컴파일/테스트 통과 확인

## 추가 피드백 반영 (상품 수정 시 상세이미지 교체 UI)
- 구현 전 학습 파일:
  - `sosos/docs/daily/learning/2026-03-03-edit-detail-image-button-learning.md`
- 이슈:
  - 상품 수정 모달에서 대표이미지 교체만 가능하고 상세이미지 교체 버튼이 보이지 않음
- 조치:
  1. 상품 수정 모달에 `교체할 상세 이미지 선택(선택)` 버튼 추가
  2. 파일 선택 상태 표시를 `대표 / 상세` 동시 표기로 변경
  3. 기존 업데이트 API 전달 로직(`descriptionImageFile`)과 연결 유지

## 추가 피드백 반영 (메인 광고 배너 별도 업로드 분리)
- 구현 전 학습 파일:
  - `sosos/docs/daily/learning/2026-03-03-main-banner-separate-upload-learning.md`
- 이슈:
  - 홈 상단 배너가 상품 대표이미지 재사용 방식이라 판매자가 광고 배너를 별도로 운영하기 어려움
- 조치:
  1. 배너 전용 백엔드 도메인/API 추가
     - `MainBanner` 엔티티, `MainBannerDto`, `MainBannerRepository`, `MainBannerService`
     - `MainBannerApiController`
       - `GET /api/v1/banners` (홈 공개 배너 조회)
       - `GET /api/v1/banners/manage` (슈퍼관리자 배너 조회)
       - `POST /api/v1/banners` (배너 업로드)
       - `DELETE /api/v1/banners/{bannerId}` (배너 삭제)
  2. 프론트 배너 API 모듈 추가
     - `frontend/src/services/bannerApi.js`
  3. 홈 배너 데이터 소스 변경
     - `frontend/src/pages/HomePage.jsx`
     - 상품 이미지 기반 캐러셀 -> 배너 API 기반 캐러셀로 전환
     - 배너 클릭 시 연결 상품(`targetProductId`)이 있으면 상세 페이지로 이동
  4. 슈퍼관리자 대시보드에 배너 관리 UI 추가
     - `frontend/src/pages/SellerDashboardPage.jsx`
     - 배너 등록 폼(제목/설명/연결 상품/노출 순서/이미지)
     - 등록 배너 목록 + 삭제 액션
  5. 검증
     - 프론트: `npm run lint`, `npm run build` 성공
     - 백엔드: `.\mvnw.cmd clean test` 성공

## 추가 피드백 반영 (SOLD OUT UX + 재고 있음 뱃지 제거)
- 구현 전 학습 파일:
  - `sosos/docs/daily/learning/2026-03-03-soldout-ux-and-size-plan-learning.md`
- 이슈:
  - 카드에 `재고 있음`이 항상 보이며 정보 가치가 낮음
  - 품절 상품 상세에서 버튼이 비활성화되어 클릭 시 안내 메시지를 받을 수 없음
- 조치:
  1. 상품 리스트 재고 뱃지 정리
     - `frontend/src/components/ProductList.jsx`
     - `재고 있음` 제거, 품절일 때만 `SOLD OUT` 표시
  2. 상품 상세 품절 액션 UX 변경
     - `frontend/src/pages/ProductDetailPage.jsx`
     - 품절 상태에서도 버튼 클릭 시 `SOLD OUT 상품입니다.` 토스트 노출
     - 재고 표시 텍스트도 품절 시 `SOLD OUT`으로 통일
  3. 서버 품절 검증 메시지 보강
     - `sosos/src/main/java/com/prosos/sosos/service/UserService.java`
       - 장바구니 담기 시 재고 0 검증 추가
       - 장바구니 주문 시 품절 포함 주문 차단 메시지 명확화
     - `sosos/src/main/java/com/prosos/sosos/service/SellerService.java`
       - 바로 주문 품절 메시지 `SOLD OUT 상품입니다.`로 통일
  4. 검증
     - 프론트: `npm run lint`, `npm run build` 성공
     - 백엔드: `.\mvnw.cmd clean test` 성공
  5. 트러블슈팅
     - `UserService.java` 저장 과정에서 UTF-8 BOM이 들어가 컴파일 에러(`illegal character: '\ufeff'`) 발생
     - BOM 제거 후 재테스트 통과

## 추가 피드백 반영 (사이즈 옵션 모델 구현)
- 구현 전 학습 파일:
  - `sosos/docs/daily/learning/2026-03-03-size-option-model-implementation-learning.md`
- 이슈:
  - 기존 one-stock 구조라 판매자 사이즈 등록/사용자 사이즈 선택/사이즈별 주문이 불가능함
- 조치:
  1. 백엔드 도메인 확장
     - `ProductOption` 엔티티 및 `ProductOptionRepository` 추가
     - `Product`에 옵션 목록 연관관계 추가
     - `Cart`에 `product_option_id` 연결 필드 추가
     - `Order`에 `size_label` 필드 추가
  2. DTO/API 확장
     - `ProductDto`에 `options`, `selectedOptionId`, `selectedSizeLabel`, `cartItemId` 추가
     - `OrderDto`에 `sizeLabel` 추가
     - 상품 등록/수정 API에서 `options` JSON 지원
     - 장바구니 담기/바로주문 API에서 `optionId` 파라미터 지원
     - 장바구니 삭제 API를 `DELETE /api/v1/products/cart/items/{cartItemId}`로 추가
  3. 서비스 로직 확장
     - 상품 등록/수정 시 사이즈 옵션 저장 및 총재고 자동 계산
     - 장바구니 담기 시 사이즈 미선택/재고 부족 검증
     - 주문 시 사이즈별 재고 차감 + 주문 레코드에 사이즈 저장
  4. 프론트 반영
     - 관리자 대시보드 상품 등록/수정에 `사이즈별 재고(예: S:10, M:5)` 입력 추가
     - 상품 상세에 사이즈 선택 UI(Chip) 추가
     - 사이즈 미선택 시 담기/주문 차단
     - 장바구니/주문내역에 선택 사이즈 표기
     - 상품 리스트에서 옵션 상품은 `바로 담기` 대신 `사이즈 선택` 유도
  5. 검증
     - 프론트: `npm run lint`, `npm run build` 성공
     - 백엔드: `.\mvnw.cmd clean test` 성공
