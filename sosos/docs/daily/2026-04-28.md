# Daily Log - 2026-04-28

## 메타 정보
- 프로젝트: SOS 리뉴얼
- 오늘 목표: AI 추천 탈옥/유해 문맥 차단 + 계절/상황 의도 매칭 보강
- 완료 기준(DoD): 외부 쇼핑몰 유도/유해 문맥 입력은 차단 응답으로 종료되고, 관련 테스트가 통과한다.

## 의사결정 1줄
- AI 안전 우선 원칙으로 외부 쇼핑몰 유도와 비인격화/폭력 문맥은 추천을 중단하고 차단 응답을 반환한다. 이유: 기능 정확도보다 안전성과 서비스 범위 준수가 우선이다.

## 사전점검 체크리스트
- [x] 구현 전 학습 문서 작성 (`docs/daily/learning/2026-04-28-ai-safety-guardrail-learning.md`)
- [x] 설명 순서 `무엇/왜/언제/어떻게` 준수
- [x] 작업 대상 파일/완료 기준 사전 명시

## 사용자 피드백 기준 기존 오류/누락
- 누락 1: 외부 쇼핑몰 유도 문구(`무신사`)가 응답에 반영될 수 있었다.
- 누락 2: 유해 문맥(`노예`)이 포함돼도 `옷 추천` 키워드 때문에 추천이 진행됐다.
- 누락 3: 판매자 키워드가 빈약한 경우 계절/상황 의도 매칭이 약했다.

## 오늘 작업
1. 입력 안전 가드레일 강화
- 수정: `src/main/java/com/prosos/sosos/service/AiStylistService.java`
- 내용
  - 외부 쇼핑몰 유도 감지(`무신사`, `29cm`, `쿠팡` 등) 시 즉시 차단
  - 유해 문맥 감지(`노예`, `인신매매`, `납치` 등) 시 즉시 차단
  - 차단 시 `blocked=true`, 추천 목록 빈 배열 반환

2. Gemini 응답 2차 필터 추가
- 수정: `src/main/java/com/prosos/sosos/service/AiStylistService.java`
- 내용
  - Gemini 답변에 외부 채널/링크 문구가 포함되면 로컬 fallback 답변으로 대체
  - 시스템 프롬프트에 외부 쇼핑몰/검색/링크 언급 금지 규칙 명시

3. 의도 기반 추천 점수 보강
- 수정: `src/main/java/com/prosos/sosos/service/AiStylistService.java`
- 내용
  - 계절 신호(`여름 -> 반팔/린넨/얇음` 등) 점수 추가
  - 상황 신호(`결혼식/출근/데이트/면접`) 점수 추가
  - `여름용` 같은 토큰은 `여름` 변형 토큰을 함께 생성해 매칭 보강

4. 테스트 보강
- 수정: `src/test/java/com/prosos/sosos/service/AiStylistServiceTest.java`
- 추가 검증
  - 외부 쇼핑몰 유도 요청 차단
  - 유해 문맥 요청 차단
  - 판매자 키워드가 없어도 여름 상의 의도를 이름/설명으로 우선 추천

5. 학습 인덱스 동기화
- 수정: `docs/daily/learning/README.md`
- 내용
  - 학습 문서 수 65개로 갱신
  - `2026-04-28-ai-safety-guardrail-learning.md` 링크 추가

## 검증 결과
- 백엔드 컴파일: 성공
  - `cd sosos && .\mvnw.cmd -DskipTests compile`
- 단위 테스트: 성공
  - `cd sosos && .\mvnw.cmd -Dtest=AiStylistServiceTest test`
  - 결과: `Tests run: 8, Failures: 0, Errors: 0`

## 제한사항/메모
- 프론트 코드 변경은 없어서 프론트 lint/build는 이번 작업 범위에서 제외했다.

## 추가 작업 (반팔/반바지 의도 인식 보강)
1. 사용자 피드백
- 문제: `반팔 추천해봐` 요청에 대해 챗봇이 "의류 코디 추천만 지원" 차단문을 반환

2. 기존 누락 원인
- `FASHION_TERMS`에 `반팔/반바지`가 없어 패션 의도로 인식되지 않았다.
- `UPPER_TERMS`/`BOTTOM_TERMS`에도 직접 매핑 단어가 부족해 TOP/BOTTOM 우선 추천이 약했다.

3. 수정 내용
- 수정: `src/main/java/com/prosos/sosos/service/AiStylistService.java`
- 내용
  - 패션 의도 단어에 `반팔`, `긴팔`, `민소매`, `반바지`, `긴바지`, `쇼츠` 추가
  - 카테고리 의도 단어에 `반팔/긴팔/민소매`(TOP), `반바지/긴바지/쇼츠`(BOTTOM) 추가

4. 테스트 보강
- 수정: `src/test/java/com/prosos/sosos/service/AiStylistServiceTest.java`
- 검증 항목
  - `반팔 추천해봐` 요청이 차단되지 않고 TOP 추천으로 이어지는지 확인

5. 검증 결과
- 단위 테스트: 성공
  - `cd sosos && .\mvnw.cmd -Dtest=AiStylistServiceTest test`
  - 결과: `Tests run: 9, Failures: 0, Errors: 0`

## 추가 작업 (검색 대용 품목 요청 대응 보강)
1. 사용자 피드백
- 요청: 챗봇이 스타일 상담뿐 아니라 검색 대용(`반바지 추천`, `긴팔티 추천`, `반팔 셔츠 추천`)으로도 자연스럽게 동작해야 함

2. 기존 누락 원인
- `셔츠`가 `티셔츠`에 부분 일치해 품목 의도가 뒤집히는 케이스가 있었다.
- 검색형 요청에서도 후속 문구가 과도하게 스타일 질문으로 흐를 수 있었다.

3. 수정 내용
- 수정: `src/main/java/com/prosos/sosos/service/AiStylistService.java`
- 내용
  - 검색형 품목 단어 사전(`DIRECT_ITEM_TERMS`) 추가
  - 품목 별칭 확장(`반팔티`, `긴팔티`, `긴바지`, `청바지`, `슬랙스`) 추가
  - 일반 `티셔츠` 요청은 반팔 우선 토큰을 추가해 상위 노출 강화
  - `셔츠`는 단어 단위 매칭으로 변경해 `티셔츠` 오탐 차단
  - 검색형 요청에서는 상품명/설명 점수 가중치 강화
  - 검색형 요청 답변은 "색상/핏/계절/예산 더 알려달라" 반복 문구 대신 간단한 조건 확장 문구로 조정

4. 테스트 보강
- 수정: `src/test/java/com/prosos/sosos/service/AiStylistServiceTest.java`
- 추가 검증
  - `티셔츠 추천해줘`에서 반팔 티셔츠가 긴팔보다 우선되는지
  - `반팔 셔츠 추천해줘`에서 반팔 셔츠가 반팔 티셔츠보다 우선되는지
  - `반바지 추천해`에서 불필요한 스타일 상세 요청 문구가 빠지는지

5. 검증 결과
- 단위 테스트: 성공
  - `cd sosos && .\mvnw.cmd -Dtest=AiStylistServiceTest test`
  - 결과: `Tests run: 12, Failures: 0, Errors: 0`

## 추가 작업 (한글 근거 주석 보강)
1. 사용자 피드백
- 요청: 사후처리 용도로 "왜 이 로직을 만들었는지"를 코드에 한글 주석으로 남겨야 함

2. 수정 내용
- 수정: `src/main/java/com/prosos/sosos/service/AiStylistService.java`
- 내용
  - 안전 정책 분기(유해 문맥/외부 쇼핑몰 차단) 목적 주석 추가
  - 검색형 품목 대응(`DIRECT_ITEM_TERMS`, `SEARCH_ALIAS_TERMS`) 목적 주석 추가
  - 모델 출력 2차 필터 대체 목적 주석 추가
  - `셔츠` vs `티셔츠` 오탐 방지(단어 단위 매칭) 목적 주석 추가

3. 문서 동기화
- 신규: `docs/daily/learning/2026-04-28-ai-comment-rationale-learning.md`
- 수정: `docs/daily/learning/README.md` (학습 문서 68개 반영)

4. 검증 결과
- 한글 깨짐 문자(Replacement Character) 검색: 없음

## 추가 작업 (반복 답답 어투 4회 대응)
1. 사용자 피드백
- 요청: 비슷한 질문을 답답한 어투로 4회 정도 반복하면 사과 + 현재 미흡함 + 가능한 활용법 안내로 대응

2. 수정 내용
- 수정: `src/main/java/com/prosos/sosos/controller/api/v1/AiStylistApiController.java`
- 내용
  - 세션 기반 반복 상태(`기준 문장/반복 횟수`) 추적 로직 추가
  - 답답 어투 키워드 감지 + 유사 질문 판별(토큰 겹침 비율) 추가
  - 4회 도달 시 사과/미흡/활용법 안내 응답 반환(`blocked=true`, 추천 목록 빈 배열)
  - 안내 응답 반환 후 카운트 초기화로 무한 반복 응답 방지

3. 테스트 보강
- 신규: `src/test/java/com/prosos/sosos/controller/api/v1/AiStylistApiControllerTest.java`
- 검증 항목
  - 답답 어투 유사 질문 4회 시 안내 응답 트리거
  - 일반 반복 질문(중립 어투)은 트리거되지 않고 기존 서비스 흐름 유지

4. 학습/인덱스 동기화
- 신규: `docs/daily/learning/2026-04-28-ai-frustrated-repeat-response-learning.md`
- 수정: `docs/daily/learning/README.md` (학습 문서 69개 반영)

5. 검증 결과
- 테스트: 성공
  - `cd sosos && .\mvnw.cmd "-Dtest=AiStylistServiceTest,AiStylistApiControllerTest" test`
  - 결과: `Tests run: 14, Failures: 0, Errors: 0`

## 추가 작업 (판매자 상품 관리 UX 전환)
1. 사용자 피드백
- 요청: 상품 관리에서 등록 폼이 항상 위에 떠 있어 수정/관리 동선이 불편하므로 목록 중심 + 등록 버튼 + 등록/수정 모달 구조로 변경 필요
- 요청: `슈퍼관리자 대시보드` 문구를 `판매자 대시보드`로 변경

2. 기존 누락 원인
- 인라인 등록 폼이 목록 위를 차지해 관리 작업 시 스크롤 이동 부담이 컸다.
- 등록 취소 동작이 명확한 "닫기/취소 모달 흐름"으로 제공되지 않았다.

3. 수정 내용
- 수정: `frontend/src/pages/SellerDashboardPage.jsx`
- 내용
  - 인라인 `신규 상품 등록` 폼 제거
  - `등록 상품 목록` 헤더에 `상품 등록` 버튼 추가
  - `상품 등록` 모달(Dialog) 추가, 취소 시 입력 내용 초기화 + 닫기 처리
  - 수정 모달 닫기 버튼을 `취소`로 정리하고 닫기 시 편집 상태 초기화
  - 대시보드 상단 타이틀 및 에러 문구를 `판매자 대시보드`로 변경
- 수정: `frontend/src/components/AppLayout.jsx`
- 내용
  - 헤더 판매자 메뉴 문구를 `판매자 대시보드`로 변경

4. 학습/인덱스 동기화
- 신규: `docs/daily/learning/2026-04-28-seller-product-modal-flow-learning.md`
- 수정: `docs/daily/learning/README.md` (학습 문서 70개 반영)

5. 검증 결과
- 프론트 린트: 성공
  - `cd frontend && npm run lint`

## 추가 작업 (판매자 대시보드 탭 분리 + 로그인 문구 정리)
1. 사용자 피드백
- 요청: `메인 광고 배너 관리`, `등록 배너 목록`, `홈 탐색 탭 관리`를 상품 관리 탭에서 분리
- 요청: 판매자 로그인 화면에 남아있는 `슈퍼관리자` 문구를 판매자 기준으로 수정

2. 수정 내용
- 수정: `frontend/src/pages/SellerDashboardPage.jsx`
- 내용
  - 탭에 `홈 관리` 추가
  - `메인 광고 배너 관리`, `등록 배너 목록`, `홈 탐색 탭 관리`를 `홈 관리` 탭으로 이동
  - `상품 관리` 탭은 등록 상품 목록 + 등록/수정 동선만 유지
- 수정: `frontend/src/pages/AdminLoginPage.jsx`
- 내용
  - 로그인/경고/오류/버튼 문구의 `슈퍼관리자` 표현을 `판매자` 기준으로 교체
  - 예: `판매자 로그인`, `판매자 계정만 판매자 대시보드에 접근할 수 있습니다.`

3. 학습/인덱스 동기화
- 신규: `docs/daily/learning/2026-04-28-seller-tab-separation-learning.md`
- 수정: `docs/daily/learning/README.md` (학습 문서 71개 반영)

4. 검증 결과
- 프론트 린트: 성공
  - `cd frontend && npm run lint`
- 프론트 소스 내 `슈퍼관리자` 문자열 검색: 없음

## 추가 작업 (홈 배너 이미지 규격 안내)
1. 사용자 피드백
- 요청: 홈 배너 이미지가 잘리는 경우가 있어 판매자 등록 화면에서 권장 픽셀 규격을 바로 알 수 있어야 함

2. 수정 내용
- 수정: `frontend/src/pages/SellerDashboardPage.jsx`
- 내용
  - 배너 등록 폼에 권장 규격 안내 추가
  - 권장: `2400x600px` (비율 `4:1`)
  - 최소: `1200x300px`
  - 가변 화면에서 가장자리 잘림 가능성을 안내하고 중앙 배치 권장 문구 추가

3. 학습/인덱스 동기화
- 신규: `docs/daily/learning/2026-04-28-home-banner-size-guide-learning.md`
- 수정: `docs/daily/learning/README.md` (학습 문서 72개 반영)

4. 검증 결과
- 프론트 린트: 성공
  - `cd frontend && npm run lint`

## 추가 작업 (홈 배너 새로고침 깜빡임 제거)
1. 사용자 피드백
- 요청: 메인 홈에서 F5 새로고침 시 배너가 오기 전에 `현재 리뉴얼 중입니다...` 문구가 잠깐 먼저 보임

2. 기존 누락 원인
- 배너 API 응답 전에도 `currentBanner`가 비어 있으면 즉시 fallback(리뉴얼 중) 문구를 렌더했다.
- 결과적으로 정상 배너가 곧 도착해도 fallback 문구가 먼저 노출되는 깜빡임이 발생했다.

3. 수정 내용
- 수정: `frontend/src/pages/HomePage.jsx`
- 내용
  - 배너 전용 로딩 상태(`bannerLoading`) 추가
  - 배너 요청 시작 시 `bannerLoading=true`, 완료 시 `false` 처리
  - 배너 영역 렌더 순서를 `로딩 UI -> 배너 -> 빈 상태`로 분리
  - 로딩 중에는 fallback 문구 대신 배너 영역 중앙 로딩 인디케이터 표시

4. 학습/인덱스 동기화
- 신규: `docs/daily/learning/2026-04-28-home-banner-refresh-flicker-learning.md`
- 수정: `docs/daily/learning/README.md` (학습 문서 73개 반영)

5. 검증 결과
- 프론트 린트: 성공
  - `cd frontend && npm run lint`
