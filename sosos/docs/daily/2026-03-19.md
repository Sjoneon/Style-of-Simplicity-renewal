# Daily Log - 2026-03-19

## 메타 정보
- 프로젝트: SOS 쇼핑몰 리뉴얼
- 오늘 우선순위: `P2`
- 오늘 목표: 실수 방지 실행 가드 추가 + 마이페이지 MVP 구현
- 목표 완료 시점(ETA): `2026-03-19 17:20`
- 완료 기준(DoD): 실행 가드로 경로/브랜치/remote 혼선을 차단하고, 마이페이지에서 사용자 정보/주문 목록이 조회되면 완료

## 의사결정 1줄
- 무엇/왜: 서버 실행 경로와 브랜치 혼선을 막기 위해 코드 기반 실행 가드를 먼저 넣고, 그 다음 마이페이지 기능 구현을 진행한다.

## 개발 사전점검 체크리스트
- [x] 구현 전에 학습용 설명 파일을 먼저 작성했다.
- [x] 설명 순서를 `무엇/왜/언제/어떻게`로 작성했다.
- [x] 어려운 용어에 쉬운 설명을 붙였다.
- [x] 오늘 작업 대상 파일 경로를 먼저 정리했다.
- [x] 완료 기준(Definition of Done)을 1줄로 명시했다.

## 구현 전 학습 파일
- `sosos/docs/daily/learning/2026-03-19-mypage-and-safe-run-guard-learning.md`
- `sosos/docs/daily/learning/2026-03-19-mypage-five-section-learning.md`
- `sosos/docs/daily/learning/2026-03-19-mypage-minimal-visual-refresh-learning.md`

## 오늘 완료한 작업
1. 실수 방지 실행 가드 추가
- `scripts/start-renewal-dev.ps1` 신규 추가
- 실행 전 자동 검사
  - 리포지토리 루트 경로 확인 (`.git`, `frontend`, `sosos`)
  - 현재 브랜치 `main` 확인
  - push remote `renewal` 확인
  - `origin` push가 활성화되어 있으면 실행 중단
  - `DB_USERNAME`, `DB_PASSWORD` 환경변수 누락 시 실행 중단
- 포트(`8085`, `5173`, `3000`) 충돌 프로세스 정리 후 프론트/백엔드 실행
- 실행 성공 시 PID/접속 URL 출력

2. 마이페이지 MVP 구현
- `frontend/src/pages/MyPagePage.jsx`
- 플레이스홀더 페이지를 실기능 화면으로 교체
  - 상단 요약 헤더 카드 추가
  - 내 계정 정보 카드(이름/이메일 또는 사업자번호)
  - 주문 상태 요약 칩(전체/결제완료/배송중/취소/반품/교환)
  - 주문 목록(최신순 정렬, 상태 필터, 새로고침)
  - 판매자 계정일 때 대시보드 이동 안내 분기

3. 사용자 피드백 반영: 마이페이지 5개 섹션 구조 확장
- 기존 누락/오류 먼저 정리
  - 누락: 쇼핑몰 표준 마이페이지 구조(혜택/찜/리뷰/계정관리) 반영 부족
  - 오류: 계정 관리에 `계정 유형` 노출이 불필요했음
- 수정 내용
  - `frontend/src/pages/MyPagePage.jsx`를 아래 순서로 재구성
    1. 주문 관리 (실데이터: `GET /api/v1/orders/me`)
    2. 혜택 관리 (준비중 카드)
    3. 찜/최근 본 (준비중 카드)
    4. 리뷰/문의 내역 (문의 실데이터: `GET /api/inquiries/user`)
    5. 계정 관리 (이름/이메일·사업자번호 + 로그아웃/고객센터 이동)
  - `계정 유형` 라인 완전 제거
  - 401 발생 시 세션 갱신 후 로그인 페이지 이동 처리 추가

4. 사용자 피드백 반영: 기능 유지 + 미니멀 디자인 리프레시
- 기존 설명의 오류/누락 먼저 명시
  - 누락: 사용자 요구사항이 `기능 변경 없이 겉모습만`인데, 이전 응답은 구조 확장 설명 중심이었다.
- 수정 내용
  - `frontend/src/pages/MyPagePage.jsx`의 데이터/이벤트 로직은 유지
  - 다크 헤더/강한 카드 톤을 제거하고 흰 배경 + 얇은 테두리 + 리스트형 레이아웃으로 변경
  - 5개 섹션과 버튼/필터/조회 동작은 동일하게 유지

## 검증 결과
- 프론트 린트: 성공
  - 명령어: `cd frontend && npm run lint`
- 프론트 빌드: 성공
  - 명령어: `cd frontend && npm run build`
  - 참고: 번들 크기 경고(500KB 초과) 1건 유지
- 백엔드 컴파일: 성공
  - 명령어: `cd sosos && .\mvnw.cmd -DskipTests compile`
- 안전 실행 스크립트: 성공
  - 명령어: `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\start-renewal-dev.ps1`
  - 결과: `http://localhost:3000`, `http://localhost:8085` 리스닝 확인

## 우선순위/ETA 점검
- P2 진행률: 진행 중
- 완료된 항목: 마이페이지(`/mypage`) 5개 섹션 MVP + 미니멀 디자인 반영
- 다음 우선순위: 알림(`/notifications`) 1차 구현

## 추가 작업(계정보안 연동)
5. 마이페이지 계정관리 실제 API 연동
- `frontend/src/pages/MyPagePage.jsx`
  - 계정관리의 준비중 안내를 실제 폼으로 교체
  - 회원정보 수정(`PUT /api/v1/users/me/profile`) 연동
  - 배송지 수정(`PUT /api/v1/users/me/address`) 연동
  - 비밀번호 변경(`POST /api/v1/users/me/password`) 연동
  - 비밀번호 변경 성공 시 세션 재동기화 후 로그인 페이지 이동
- `sosos/docs/daily/learning/2026-03-19-mypage-account-security-integration-learning.md` 학습 문서 추가

## 추가 검증 결과(계정보안 연동)
- 프론트 린트: 성공
  - 명령어: `cd frontend && npm run lint`
- 프론트 빌드: 성공
  - 명령어: `cd frontend && npm run build`
  - 참고: 번들 크기 경고(500KB+)는 기존과 동일
- 백엔드 컴파일: 성공
  - 명령어: `cd sosos && .\mvnw.cmd -DskipTests compile`

## 추가 작업(마이페이지 정보 구조 정리)
6. 마이페이지 섹션 접기/펼치기 구조 반영
- `frontend/src/pages/MyPagePage.jsx`
  - 5개 섹션(주문/혜택/찜·최근본/리뷰·문의/계정관리)을 클릭형 토글 UI로 전환
  - 초기에는 요약 정보만 노출하고, 클릭 시 상세 영역 노출
  - 기존 기능(주문 필터/문의 요약/계정 수정 폼)은 유지
- `sosos/docs/daily/learning/2026-03-19-mypage-accordion-structure-learning.md` 학습 문서 추가

## 추가 검증 결과(마이페이지 구조 정리)
- 프론트 린트: 성공
  - 명령어: `cd frontend && npm run lint`
- 프론트 빌드: 성공
  - 명령어: `cd frontend && npm run build`
  - 참고: 번들 크기 경고(500KB+)는 기존과 동일

## 추가 작업(마이페이지 배송지 분리)
7. 마이페이지 섹션 재구성
- `frontend/src/pages/MyPagePage.jsx`
  - `5. 배송지 관리` 섹션 신설(배송지 수정 폼 이동)
  - 기존 `5. 계정 관리`를 `6. 계정 관리`로 조정
  - 계정관리의 회원정보 수정은 이름/연락처 중심으로 정리
  - 배송지는 5번 섹션에서만 수정하도록 분리

## 추가 검증 결과(배송지 분리)
- 프론트 린트: 성공
  - 명령어: `cd frontend && npm run lint`
- 프론트 빌드: 성공
  - 명령어: `cd frontend && npm run build`
  - 참고: 번들 크기 경고(500KB+)는 기존과 동일

## 추가 작업(문서 정합성 정리)
8. 핵심 MD 문서 동기화
- `README.md`: 마이페이지 상태를 대기 -> 완료로 갱신
- `sosos/docs/work-summary.md`: 작성일/마이페이지 현재 상태 설명 최신화
- `sosos/docs/daily/today-action-board.md`: 아코디언/배송지 분리 완료 체크 반영
- `sosos/docs/daily/learning/README.md`: 학습 문서 개수/목록 최신화

## 추가 작업(알림 1차 구현)
9. 알림 페이지(`/notifications`) 파생형 알림 구현
- `frontend/src/services/notificationApi.js` 신규
  - 재입고/할인/주문상태 변경 알림 계산 로직 추가
  - 사용자별 스냅샷/알림 이력/읽음 상태를 localStorage에 저장
- `frontend/src/pages/NotificationsPage.jsx`
  - 플레이스홀더를 실제 알림 목록 페이지로 교체
  - 유형 필터(전체/재입고/할인/주문 상태), 미읽음 표시, 전체 읽음 처리
  - 401 발생 시 세션 갱신 후 로그인 페이지 이동
- `sosos/src/main/java/com/prosos/sosos/dto/OrderDto.java`
  - 알림 연계를 위해 `productId` 필드 추가

## 추가 검증 결과(알림 1차)
- 프론트 린트: 성공
  - 명령어: `cd frontend && npm run lint`
- 프론트 빌드: 성공
  - 명령어: `cd frontend && npm run build`
  - 참고: 번들 크기 경고(500KB+)는 기존과 동일
- 백엔드 컴파일: 성공
  - 명령어: `cd sosos && .\\mvnw.cmd -DskipTests compile`

## 추가 작업(찜 기능 1차 구현)
10. 찜 기능(DB 저장형) 구현
- 백엔드
  - `sosos/src/main/java/com/prosos/sosos/model/WishlistItem.java` 신규
  - `sosos/src/main/java/com/prosos/sosos/repository/WishlistItemRepository.java` 신규
  - `sosos/src/main/java/com/prosos/sosos/controller/api/v1/WishlistApiController.java` 신규
  - API: `GET /api/v1/wishlist`, `GET /api/v1/wishlist/{productId}/status`, `POST /api/v1/wishlist/{productId}`, `DELETE /api/v1/wishlist/{productId}`
- 프론트
  - `frontend/src/services/wishlistApi.js` 신규
  - `frontend/src/pages/ProductDetailPage.jsx`: 찜 토글 버튼 연동
  - `frontend/src/pages/MyPagePage.jsx`: 찜 목록 조회/해제/상품 이동 연동
- 학습 문서
  - `sosos/docs/daily/learning/2026-03-19-wishlist-mvp-learning.md`

## 추가 검증 결과(찜 기능)
- 백엔드 컴파일: 성공
  - `cd sosos && .\\mvnw.cmd -DskipTests compile`
- 프론트 린트: 성공
  - `cd frontend && npm run lint`
- 프론트 빌드: 성공
  - `cd frontend && npm run build`
  - 참고: 번들 경고(500KB+)는 기존과 동일

## 추가 작업(마이페이지 배송지 카카오 주소검색)
11. 배송지 입력 UX 개선
- `frontend/src/utils/loadKakaoPostcode.js` 신규
  - 카카오 우편번호 스크립트 동적 로드 + 팝업 오픈 유틸 추가
- `frontend/src/pages/MyPagePage.jsx`
  - 배송지 폼을 `우편번호/기본 주소/상세 주소` 3필드로 분리
  - `주소 검색` 버튼으로 카카오 우편번호 팝업 연동
  - 저장 시 `(우편번호) 기본주소 상세주소` 형태로 합쳐 기존 API(`PUT /api/v1/users/me/address`)에 전송

## 추가 검증 결과(배송지 주소검색)
- 프론트 린트: 성공
  - `cd frontend && npm run lint`
- 프론트 빌드: 성공
  - `cd frontend && npm run build`
  - 참고: 번들 경고(500KB+)는 기존과 동일

## 추가 작업(SQL 스키마 보강)
12. 기존 SQL과 최신 엔티티 정합화
- `sosos/db/seed/mydb.sql` 최신화
  - 누락 테이블 반영: `discovery_tabs`, `product_options`, `main_banners`, `wishlist_items`
  - 누락 컬럼 반영: `products.show_in_*`, `products.discovery_tab_keys`, `orders.size_label`, `cart_items.product_option_id`
- `sosos/db/seed/mydb_upgrade_2026_03_19.sql` 신규
  - 기존 DB 업그레이드용 `ALTER/CREATE` 보강 쿼리 추가(데이터 유지 목적)

## 추가 작업(배송지 저장 후 미반영 이슈 수정)
13. 세션 사용자 DTO 보강
- `sosos/src/main/java/com/prosos/sosos/dto/UserSessionDto.java`
  - 사용자 필드 `phone`, `address` 추가
  - `fromUser()`에서 전화번호/주소 매핑
- 원인: 저장 API는 성공했지만 `/api/v1/users/me` 응답에 주소/연락처가 없어 프론트 `refreshSession()` 시 빈 값으로 덮임

## 추가 검증 결과(배송지 미반영 이슈)
- API 시나리오 검증 성공
  - 회원가입 -> 로그인 -> `PUT /api/v1/users/me/address` -> `GET /api/v1/users/me`
  - 확인: `me.data.address`, `me.data.phone` 정상 반환
