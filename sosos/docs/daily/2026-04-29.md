# Daily Log - 2026-04-29

## 메타 정보
- 프로젝트: SOS 리뉴얼
- 오늘 목표: 홈 상품 리스트 밀도/미리보기 개선 1차 적용
- 완료 기준(DoD): 카드 밀도 개선, hover 상세 미리보기, 판매자 등록 경고가 실제 화면에서 동작한다.

## 의사결정 1줄
- 빠른 체감 개선을 위해 백엔드 스키마 확장 없이 프론트에서 즉시 적용 가능한 카드 밀도/정보 재배치/상세 미리보기/등록 경고를 먼저 반영한다.

## 사전점검 체크리스트
- [x] 구현 전 학습 문서 작성 (`docs/daily/learning/2026-04-29-product-list-density-and-preview-learning.md`)
- [x] 설명 순서 `무엇/왜/언제/어떻게` 준수
- [x] 작업 대상 파일/완료 기준 사전 명시

## 추가 작업 (홈 상품 리스트 1차 개선)
1. 사용자 피드백
- 요청: 홈 카드가 너무 커서 비교 효율이 떨어지고, 상세페이지 정보 부족으로 판단이 어렵다.
- 요청: 먼저 1차 적용안을 실제로 확인 가능하게 반영

2. 수정 내용
- 수정: `frontend/src/components/ProductList.jsx`
- 내용
  - 그리드 밀도 재조정: 데스크톱 기준 최대 5열(`columns` + `size` 조합)
  - 카드 컴팩트화: 여백/그림자/폰트/버튼 크기 조정
  - 정보 재배치: 카테고리, 판매수, 옵션수/재고를 리스트에서 즉시 확인 가능하도록 정리
  - hover 미리보기: `descriptionImageUrl`이 있으면 마우스 hover 시 상세 이미지로 전환
  - 상세 이미지가 없을 때 `상세 이미지 미등록` 안내 표시
- 수정: `frontend/src/pages/SellerDashboardPage.jsx`
- 내용
  - 상품 등록 모달에서 상세 이미지 미선택 시 경고 Alert 표시
  - 등록 시 상세 이미지가 없으면 최종 확인(confirm) 후 진행하도록 경고 절차 추가

3. 학습/인덱스 동기화
- 신규: `docs/daily/learning/2026-04-29-product-list-density-and-preview-learning.md`
- 수정: `docs/daily/learning/README.md` (학습 문서 74개 반영)

4. 검증 결과
- 프론트 린트: 성공
  - `cd frontend && npm run lint`

## 추가 작업 (사용자 피드백 2차 반영: 카드 단순화 + 찜 + 세일)
1. 사용자 피드백
- 요청: 소비자에게 `상세 이미지 미등록` 노출 불필요
- 요청: 카드 내 `상세 보기/바로담기` 버튼 제거, 카드 클릭 중심 동선 선호
- 요청: 카드에 브랜드/하트(찜) 중심 표현
- 요청: 세일 가격 기능은 판매자/소비자 양쪽에서 동작해야 완성

2. 기존 누락 원인
- 1차안에서 소비자 카드에 운영성 경고 문구가 남아 있었다.
- 카드 CTA 버튼이 많아 리스트 스캔보다 액션 분기가 먼저 보였다.
- 브랜드/세일 가격 정보 구조가 없어 머천다이징 표현력이 약했다.

3. 수정 내용
- 수정: `frontend/src/components/ProductList.jsx`
- 내용
  - 소비자 카드에서 `상세 이미지 미등록` 문구 제거
  - `상세 보기/바로담기` 버튼 제거, 카드 전체 클릭으로 상세 진입 통일
  - 카드 정보를 `브랜드명 + 상품명 + 가격(세일 시 할인율/정상가)` 중심으로 재배치
  - 이미지 우하단 하트 아이콘으로 찜 토글 제공
  - hover 시 상세 이미지 미리보기 유지
- 수정: `frontend/src/pages/HomePage.jsx`
- 내용
  - 홈에서 찜 목록 로드(`fetchMyWishlist`) 및 카드 찜 토글(`toggleWishlist`) 연결
  - 로그인 전/권한 불일치 처리와 토스트 메시지 처리
- 수정: `frontend/src/pages/ProductDetailPage.jsx`
- 내용
  - 브랜드명 표시
  - 세일 시 할인율/정상가(취소선) 표시
- 수정: `frontend/src/services/productApi.js`
- 내용
  - 상품 등록/수정 시 `originalPrice` 전송 추가
- 수정: `sosos/src/main/java/com/prosos/sosos/model/Product.java`
- 수정: `sosos/src/main/java/com/prosos/sosos/dto/ProductDto.java`
- 수정: `sosos/src/main/java/com/prosos/sosos/service/SellerService.java`
- 내용
  - `originalPrice`(정상가) 필드 추가 및 유효성 검증(정상가 >= 판매가)
  - 리스트용 `sellerName`(브랜드명) 전달

4. 주의사항
- dev 프로필은 `ddl-auto=update`라 `original_price` 컬럼이 자동 반영된다.
- prod 프로필은 `ddl-auto=validate`이므로 운영 배포 전 DB 컬럼 마이그레이션이 필요하다.

5. 검증 결과
- 프론트 린트: 성공
  - `cd frontend && npm run lint`
- 백엔드 컴파일: 성공
  - `cd sosos && .\mvnw.cmd -DskipTests compile`

## 추가 작업 (DB 점검 및 마이그레이션 조치)
1. 사용자 피드백
- 요청: 찜/세일 기능에서 DB 고려 누락이 없는지 점검하고 필요한 조치를 반영

2. 점검 결과
- `dev`: `ddl-auto=update`라 로컬에서 컬럼/테이블 자동 반영 가능
- `prod`: `ddl-auto=validate`라 DB 스키마 누락 시 백엔드 기동 실패
- 세일 기능은 `products.original_price` 컬럼이 운영 DB에 선반영되어야 함
- 찜 기능은 `wishlist_items` 테이블/제약/인덱스가 환경별로 누락될 수 있어 점검 필요

3. 조치 내용
- 신규 SQL: `docs/deployment/sql/2026-04-29-sale-wishlist.sql`
  - `products.original_price` 조건부 추가
  - `wishlist_items` 생성
  - 찜 조회 성능용 인덱스(`user_id, created_at`, `product_id`) 보강
- 신규 가이드: `docs/deployment/db-migration-sale-wishlist.md`
  - 실행 순서(백업 → 스테이징 → 운영)
  - 검증 쿼리 및 체크 포인트 정리
- 기존 문서 보강:
  - `docs/deployment/env-standard.md`에 DB 마이그레이션 규칙 추가
  - `docs/deployment/ec2-iam-role-rehearsal.md`에 배포 전 DB 마이그레이션 단계 추가

4. 추가 검증
- `prod + validate` 기동 점검: 성공
  - 확인 방식: `SPRING_PROFILES_ACTIVE=prod`, `APP_STORAGE_TYPE=local`, 별도 포트로 임시 기동
  - 결과: 스키마 검증 단계에서 기동 실패 없음 (`Started SososApplication` 확인)

## 추가 작업 (한글 주석 스타일 정렬 + 깨짐 검사)
1. 사용자 피드백
- 요청: 어제와 같은 스타일의 한글 근거 주석 반영 + 한글 깨짐 여부 점검

2. 기존 누락 원인
- 최근 찜/세일/카드 동선 코드에 "왜 이런 분기를 둔 건지" 한글 근거가 일부 부족했다.

3. 수정 내용
- 수정: `frontend/src/components/ProductList.jsx`
- 내용
  - hover 상세 미리보기 의도 주석 추가
  - 카드 클릭/찜 클릭 이벤트 분리 이유 주석 추가
  - 세일 노출 기준(정상가 > 판매가) 주석 추가
- 수정: `frontend/src/pages/HomePage.jsx`
- 내용
  - 찜 API 호출 대상(일반 사용자만) 제한 이유 주석 추가
  - 상품 단위 찜 로딩 상태 분리 이유 주석 추가
- 수정: `sosos/src/main/java/com/prosos/sosos/service/SellerService.java`
- 내용
  - 정상가=판매가일 때 null 정규화하는 정책 이유 주석 추가

4. 문서 동기화
- 신규: `docs/daily/learning/2026-04-29-korean-comment-style-alignment-learning.md`
- 수정: `docs/daily/learning/README.md` (학습 문서 76개 반영)

5. 검증 결과
- 프론트 린트: 성공
  - `cd frontend && npm run lint`
- 한글 깨짐 문자(U+FFFD) 검색:
  - 대상 변경 파일 기준 0건

## Additional Work (Folder-based Product Reseed + Category Cleanup)
1. Request
- Delete all existing site products and re-register from `sosos/uploads` folder groups.
- Keep category mapping by folder (`OUTER`, `TOP`, `BOTTOMS`, `SHOES`, `BAG_ACC`).
- Mark BEST picks as random 3 items per folder.
- Use common detail image: `uploads/test-Detailed-explanation.png`.
- Remove `SET CLOTHES` from consumer/seller category UI.

2. Backend changes
- `SellerService.deleteProduct` now clears dependent data first (banner, inquiry, recent view, wishlist, cart, review, order) before product delete.
- Added repository delete methods:
  - `ProductReviewRepository.deleteByProduct_Id`
  - `OrderRepository.deleteByProduct_Id`
  - `CartRepository.deleteByProduct_Id`
  - `WishlistItemRepository.deleteByProductId`
  - `RecentProductViewRepository.deleteByProductId`
  - `MainBannerRepository.deleteByTargetProductId`
  - `InquiryRepository.deleteByProductId`
- `ProductApiController` updated:
  - renamed JSON params to `optionsJson`, `discoveryTabKeysJson`
  - added CSV fallback parser for keywords and discovery tab keys

3. Frontend changes
- `HomePage.jsx`
  - removed `SET CLOTHES` from category tabs
  - `BEST` now works as virtual filter via `discoveryTabKeys` containing `best`
- `SellerDashboardPage.jsx`
  - category options updated to `OUTER, TOP, BOTTOMS, SHOES, BAG_ACC`
- `productApi.js`
  - switched to new param names `optionsJson`, `discoveryTabKeysJson`

4. Data reseed script
- Added `scripts/reseed-products-from-uploads.ps1`
  - deletes existing products
  - registers 57 products from upload folders
  - assigns image + common description image
  - sets category, price, keywords, discovery tabs
  - randomly marks BEST picks: 3 per folder (total 15)

5. Verification
- Backend compile: `cd sosos && .\mvnw.cmd -q -DskipTests compile`
- Frontend lint: `cd frontend && npm run lint`
- API result:
  - total products: 57
  - category counts: OUTER 11 / TOP 9 / BOTTOMS 12 / SHOES 11 / BAG_ACC 14
  - BEST count: 15 (3 per category folder)

## Additional Work (Size Option Visibility Fix)
1. Issue
- Consumer product detail page did not show size selection before order.

2. Root cause
- Re-seeded products were created without `options`, so detail UI condition (`hasOptions`) stayed false.

3. Fix
- Backend: `ProductApiController.parseOptionDtos` now supports CSV fallback (`M:3,L:4,XL:3`) for multipart input.
- Script: `scripts/reseed-products-from-uploads.ps1` now sends `optionsJson` for `OUTER/TOP/BOTTOMS/SHOES` and skips options for `BAG_ACC`.
- Re-seeded all products again after backend restart.

4. Validation
- Category option coverage:
  - OUTER/TOP/BOTTOMS/SHOES: all products have options.
  - BAG_ACC: all products have no options.
- Final product count remains 57.

## 추가 작업 (문의 카테고리 확장 + 이미지 첨부 + 주의문구)
1. 사용자 요청
- 소비자 문의를 카테고리(배송/주문결제/취소교환환불/회원정보/상품확인/서비스/사용 문의)로 분류하고, 판매자도 카테고리 기준으로 정리/답변 가능하게 개선
- 소비자 문의에 이미지 첨부(크기/용량 제한) 추가
- 문의 전 교환/환불 주의 문구 노출

2. 기존 누락
- 문의 엔티티에 카테고리/첨부 이미지 필드가 없어 우선순위 분류와 증빙 전달이 어려웠다.
- 판매자 Q&A 화면은 미답변 필터만 있어서 카테고리별 정리 속도가 낮았다.

3. 수정 내용
- 백엔드
  - `Inquiry` 엔티티에 `category`, `imageUrl` 필드 추가
  - `InquiryDto`에 `productId`, `category`, `imageUrl` 확장
  - `InquiryController` 개선
    - 문의 등록 `JSON + multipart/form-data` 동시 지원
    - 카테고리 정규화 로직 추가(한글/영문 별칭 -> 저장 코드)
    - 첨부 이미지 서버 검증 추가(형식/용량/해상도)
    - 이미지 저장 경로: `inquiries/` 디렉터리
- 프론트(소비자)
  - `CustomerCenterPage.jsx`
    - 카테고리 선택 버튼 추가
    - 교환/환불 주의문구 고정 노출 + 확인 스위치
    - 문의 이미지 첨부/미리보기/제거 기능 추가
    - 상태/카테고리 필터 추가
    - 목록에 카테고리/첨부 이미지 표시
- 프론트(판매자)
  - `SellerDashboardPage.jsx`
    - Q&A 카테고리 필터 추가
    - 문의 카드에 카테고리/첨부 이미지 표시
- API 유틸
  - `inquiryApi.js`
    - 카테고리 상수/라벨/정규화 헬퍼 추가
    - 문의 등록을 `FormData` 전송으로 변경

4. DB/배포 조치
- 신규 SQL: `docs/deployment/sql/2026-04-29-inquiry-category-image.sql`
  - `inquiries.category`, `inquiries.image_url` 컬럼 추가
  - 기존 데이터 `category` 기본값 보정(`SERVICE`)

5. 검증 결과
- 프론트 린트: 성공
  - `cd frontend && npm run lint`
- 백엔드 컴파일: 성공
  - `cd sosos && .\mvnw.cmd -DskipTests compile`
- 한글 깨짐 문자(U+FFFD) 점검: 변경 파일 기준 0건

## 추가 작업 (다음 단계 실행: DB 반영 + 재기동 + 스모크 테스트)
1. 실행 순서
- `docs/deployment/sql/2026-04-29-inquiry-category-image.sql` 로컬 DB 적용
- 백엔드/프론트 기존 프로세스 종료 후 재기동
- 사용자 문의 등록(카테고리 + 이미지) 및 판매자 조회 스모크 테스트

2. DB 반영 결과
- 적용 도구: `mysqlsh` (mysql CLI 미설치 환경)
- 호환 이슈 해결: `ADD COLUMN IF NOT EXISTS` 미지원 버전 대응으로 동적 SQL 방식 적용
- 최종 컬럼 상태:
  - `inquiries.category` -> `varchar(40)`
  - `inquiries.image_url` -> `varchar(1024)`

3. 서버 재기동 결과
- 백엔드: `http://localhost:8085` (포트 8085 LISTEN 확인)
- 프론트: `http://localhost:3000` (포트 3000 LISTEN 확인)
- 헬스체크: `/actuator/health` 응답 `{"status":"UP"}`

4. 문의 스모크 테스트 결과
- 테스트 사용자 로그인 후 문의 등록(multipart): 성공
  - 카테고리: `SITE_USAGE`
  - 첨부 이미지 URL 생성: `/images/inquiries/..._test-Detailed-explanation.png`
- 사용자 문의 목록(`/api/inquiries/user`)에서 카테고리/이미지 필드 확인
- 판매자 로그인 후 전체 문의(`/api/inquiries`)에서 동일 데이터 확인

5. 참고 경로
- 로그 파일: `runtime-logs/backend.log`, `runtime-logs/frontend.log`
- 쿠키/요청 파일: `runtime-logs/user-cookie.txt`, `runtime-logs/seller-cookie.txt`
