# Daily Log - 2026-02-22

## 메타 정보
- 프로젝트: SOS 쇼핑몰 리뉴얼
- 작성일: 2026-02-22
- 소요 시간: 진행 중
- 오늘 단계: 1순위 주문/재고 무결성 보강
- 오늘 한 줄 목표: 주문 생성과 재고 차감을 트랜잭션으로 묶고 `Order.quantity` 저장 정책을 고정한다.
- 오늘 우선순위: `P1`
- 목표 완료 시점(ETA): `2026-02-22 14:37`
- 학습 설명 파일: `docs/daily/learning/2026-02-22-order-inventory-learning.md`

## 개발 사전점검 체크리스트
- [x] 구현 전에 학습용 설명 파일을 먼저 만들었는가
- [x] 설명 순서를 `무엇/왜/언제/어떻게`로 썼는가
- [x] 어려운 용어에 쉬운 설명을 붙였는가
- [x] 오늘 작업의 대상 파일 경로를 미리 적었는가
- [x] 완료 기준(Definition of Done)을 1줄로 적었는가

## 데일리 스크럼
### 오늘 완료한 작업
- 주문/재고 무결성 보강 대상 코드 탐색 완료 (`ProductController`, `SellerService`, `UserService`, `OrderDto`)
- 무결성 이슈 재확인: 트랜잭션 부재, `Order.quantity` 미설정, 주문 DTO 수량 매핑 오류
- `ProductRepository`에 비관적 락 조회(`findByIdForUpdate`) 추가
- `UserService.purchaseCart` 트랜잭션 메서드 신설 (`재고 차감 + 주문 저장 + 장바구니 정리`)
- `SellerService.processPurchase` 트랜잭션화 및 `Order.quantity`/`totalAmount` 저장 일관화
- `ProductController.purchase`의 컨트롤러 직접 DB 처리 제거, 서비스 호출로 단순화
- `OrderDto` 수량 매핑을 `order.getQuantity()`로 수정

### 진행 중인 작업
- 없음

### 막히거나 모르는 것 (블로커)
- 없음

### 내일 할 것
- 적용 결과 기반 수동 스모크 시나리오 정리

### 우선순위/ETA 점검
- P1 진행률: 100% (P1-1, P1-2, P1-3 코드 반영 완료)
- P2 진행률: 0%
- ETA 변동 여부(있음/없음): 없음

## 검증 결과
- `mvnw clean compile` 성공
- `mvnw test` 성공 (총 6개 테스트, 실패/에러 0)
- 검증 중 발견 이슈 수정: `UserService.purchaseCart`에서 불변 리스트 `sort()` 예외 가능성 → 리스트 복사 후 정렬로 보완

## 작업 상세
### 작업 1
- 파일 경로: `sosos/src/main/java/com/prosos/sosos/repository/ProductRepository.java`
- 코드/설정 스니펫:
```text
@Lock(LockModeType.PESSIMISTIC_WRITE)
@Query("select p from Product p where p.id = :id")
Optional<Product> findByIdForUpdate(@Param("id") Long id);
```
- 작성 이유:
동시 구매 요청에서 같은 재고를 중복 차감하는 경쟁 상태를 줄이기 위해.

### 작업 2
- 파일 경로: `sosos/src/main/java/com/prosos/sosos/service/UserService.java`
- 코드/설정 스니펫:
```text
@Transactional
public void purchaseCart(User user) { ... }
```
- 작성 이유:
장바구니 결제의 핵심 DB 작업을 하나의 트랜잭션으로 묶어 부분 반영을 막기 위해.

### 작업 3
- 파일 경로: `sosos/src/main/java/com/prosos/sosos/service/SellerService.java`, `sosos/src/main/java/com/prosos/sosos/controller/ProductController.java`, `sosos/src/main/java/com/prosos/sosos/dto/OrderDto.java`
- 코드/설정 스니펫:
```text
order.setQuantity(orderQuantity);
this.quantity = order.getQuantity();
```
- 작성 이유:
주문 시점 수량을 명시 저장/조회해 주문 데이터와 현재 재고 값의 혼선을 제거하기 위해.
