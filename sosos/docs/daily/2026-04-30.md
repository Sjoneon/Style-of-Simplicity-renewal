# Daily Log - 2026-04-30

## 메타 정보
- 프로젝트: SOS 리뉴얼
- 오늘 목표: 문의 목록 번호 표기를 사용자 친화적으로 정리
- 완료 기준(DoD): 소비자 문의 목록에서 전역 ID를 숨기고 화면 순번으로 표시된다.

## 의사결정 1줄
- 사용자 혼란을 줄이기 위해 DB 전역 ID 노출 대신 현재 화면 기준 순번(`내 문의 1, 2, 3...`)을 사용한다.

## 사전점검 체크리스트
- [x] 구현 전 학습 문서 작성 (`docs/daily/learning/2026-04-30-inquiry-display-sequence-learning.md`)
- [x] 설명 순서 `무엇/왜/언제/어떻게` 준수
- [x] 작업 대상 파일/완료 기준 사전 명시

## 작업 내용
1. 사용자 피드백
- 문의 목록에서 같은 계정인데 문의 번호가 `#1` 다음 `#5`로 보여 혼란스럽다는 피드백

2. 원인
- 문의 번호는 사용자별 순번이 아니라 `inquiries` 테이블의 전역 자동증가 PK(`inquiry.id`)를 그대로 노출하고 있었다.

3. 수정
- 파일: `frontend/src/pages/CustomerCenterPage.jsx`
- 변경:
  - 목록 렌더링을 `map((inquiry, index) => ...)`로 변경
  - 제목 표기 `#{inquiry.id}` -> `내 문의 {index + 1}`
  - 첨부 이미지 alt도 동일하게 화면 순번 기준으로 변경

4. 검증
- 프론트 린트: 성공
  - `cd frontend && npm run lint`

5. 문서 동기화
- 추가: `docs/daily/learning/2026-04-30-inquiry-display-sequence-learning.md`
- 수정: `docs/daily/learning/README.md`

---

## 추가 작업 - 관리자 API 보안패치

### 메타 정보
- 오늘 목표: 판매자 전용 API를 세션 권한 + 소유권 기준으로 차단
- 완료 기준(DoD): 비로그인/일반사용자/타 판매자 요청이 관리자 API에서 차단되고, 기존 사용자 기능이 회귀 없이 동작한다.

### 의사결정 1줄
- 프론트 경로 가드만으로는 API 직접 호출을 막을 수 없어서, 서버 컨트롤러/서비스에 권한 검증을 이중 적용한다.

### 사전점검 체크리스트
- [x] 구현 전 학습 문서 작성 (`docs/daily/learning/2026-04-30-admin-api-security-hardening-learning.md`)
- [x] 설명 순서 `무엇/왜/언제/어떻게` 준수
- [x] 작업 대상 파일/완료 기준 사전 명시

### 작업 내용
1. 관리자 API 접근제어 보강
- `OrderApiController`, `ProductApiController`, `InquiryController`에 판매자 세션 필수 검증 추가
- `/api/v1/orders/seller`의 `sellerId` 쿼리 파라미터 신뢰 제거 (세션 판매자 ID만 사용)
- 상품 등록 시 클라이언트 `sellerId` 신뢰 제거 (`addProductForSeller` 경유)

2. 서비스 레벨 소유권 검증 추가
- `SellerService`에 주문/상품 소유권 검증 로직 추가
- 타 판매자 주문 상태 변경, 타 판매자 상품 수정/삭제 차단

3. 레거시 API 보강
- `OrderController`, `ProductController`, `SellerController`에도 동일한 판매자 권한 검증 적용

4. 프론트 연동 조정
- `frontend/src/services/orderApi.js`: 판매자 주문 조회에서 `sellerId` 파라미터 제거
- `frontend/src/services/productApi.js`: 상품 등록 FormData에서 `sellerId` 제거

5. 보안 회귀 테스트 추가
- `OrderApiControllerSecurityTest`
- `ProductApiControllerSecurityTest`
- `InquiryControllerSecurityTest`

### 검증
- 백엔드 테스트: 성공
  - `cd sosos && .\\mvnw.cmd test -q`
- 프론트 정적검증: 성공
  - `cd frontend && npm run lint`
  - `cd frontend && npm run build`

### 문서 동기화
- 추가: `docs/daily/learning/2026-04-30-admin-api-security-hardening-learning.md`

---

## 추가 작업 - AI 스타일 추천 프롬프트/의도 판정 안정화

### 메타 정보
- 오늘 목표: "여름 상하의 추천" 같은 자연어 요청이 차단되거나 하의만 추천되는 문제를 해소
- 완료 기준(DoD): 상하의 의도 요청에서 TOP/BOTTOM 조합 추천이 안정적으로 나오고, 외부몰/유해 문맥 차단은 유지된다.

### 의사결정 1줄
- 복잡한 분기 프롬프트를 간결한 규칙 프롬프트로 줄이고, 서버측 의도 파싱/후보 재정렬을 강화해 모델 오답을 줄인다.

### 사전점검 체크리스트
- [x] 구현 전 학습 문서 확인 (`docs/daily/learning/2026-04-30-ai-prompt-optimization-learning.md`)
- [x] 설명 순서 `무엇/왜/언제/어떻게` 준수
- [x] 작업 대상 파일/완료 기준 사전 명시

### 작업 내용
1. `AiStylistService` 재정비
- 인코딩 파손/문자열 깨짐 상태를 제거하고 파일을 UTF-8 기준으로 재작성
- 짧은 검색형 질의(예: `반팔 추천`)도 패션 의도로 인식하도록 `containsQuickFashionIntent` 보강
- `상하의` 요청 시 `TOP + BOTTOM`이 함께 나오도록 `rebalanceCandidatesByIntent` 적용

2. 프롬프트 단순화
- Gemini system prompt를 5개 핵심 규칙으로 축약
- 외부몰/링크 언급 금지, 요청 카테고리 준수, TOP+BOTTOM 요청 시 카테고리 포함을 명시

3. 후보 점수화/분류 보강
- 상품명/설명/키워드 매칭 점수 구조 정리
- 잘못 저장된 카테고리를 상품명 기반으로 보정(`BAG/SHOES/ACC/TOP/BOTTOM/OUTER`)
- `셔츠` vs `티셔츠`, `반팔` 우선도 가중치 보정

4. 테스트 재작성 및 검증
- 파일: `sosos/src/test/java/com/prosos/sosos/service/AiStylistServiceTest.java`
- 주요 시나리오:
  - 의류만 요청 시 비의류 제외
  - 여름 상하의 요청 시 TOP/BOTTOM 동시 추천
  - 반팔/티셔츠/반팔셔츠 의도 인식
  - 외부몰 요청 및 유해 문맥 차단

### 검증
- 백엔드 테스트: 성공
  - `cd sosos && .\\mvnw.cmd -q -Dtest=AiStylistServiceTest test`
- 백엔드 컴파일: 성공
  - `cd sosos && .\\mvnw.cmd -q -DskipTests compile`

### 문서 동기화
- 학습 문서 사용: `docs/daily/learning/2026-04-30-ai-prompt-optimization-learning.md`

---

## 추가 작업 - AI 카테고리 조합 일반화

### 메타 정보
- 오늘 목표: 특정 조합 하드코딩이 아니라 다양한 카테고리 조합/제외 표현을 공통 처리
- 완료 기준(DoD): 혼합 조합 요청은 요청 카테고리를 함께 추천하고, 제외 요청은 반영된다.

### 의사결정 1줄
- 조합 케이스를 계속 추가하는 방식 대신, `의도 파싱 + 범용 재정렬` 구조로 변경해 확장성을 확보한다.

### 작업 내용
1. 의도 파싱 일반화
- `includeCategory(...)` 도입
- 카테고리 요청 감지와 제외 표현 감지를 한 함수에서 처리

2. 제외 표현 반영
- `빼고/제외/말고/없이`를 감지하는 `containsExcludedPhrase(...)` 추가
- 예: `신발은 빼고 상하의 추천`에서 SHOES 제외

3. 후보 재정렬 범용화
- 기존: TOP/BOTTOM 특화 재정렬
- 변경: 요청된 모든 카테고리의 상위 후보를 먼저 배치한 뒤 점수 순 후보를 이어붙이는 방식

4. 테스트 추가
- `shouldIncludeAllRequestedMixedCategoriesWhenAvailable`
- `shouldRespectExclusionPhraseForCategory`

### 검증
- `cd sosos && .\\mvnw.cmd clean -DskipTests compile` 성공
- `cd sosos && .\\mvnw.cmd -q -Dtest=AiStylistServiceTest test` 성공

### 문서 동기화
- 추가: `docs/daily/learning/2026-04-30-ai-combination-generalization-learning.md`

---

## 추가 작업 - 깨진 한글 주석/문구 복구

### 메타 정보
- 오늘 목표: 서비스 코드에 남아 있는 모지바케 한글 주석/예외 문구를 즉시 복구
- 완료 기준(DoD): 깨진 문자열이 제거되고 컴파일이 정상 통과한다.

### 의사결정 1줄
- 비즈니스 로직 변경 없이 텍스트(주석/메시지)만 교체해 안정성을 유지하면서 가독성을 회복한다.

### 작업 내용
1. 대상 식별
- `SellerService`에서 깨진 한글 패턴 다수 확인

2. 복구
- 주석 섹션 제목(회원가입/상품 등록/주문/문의/키워드 등) 정상화
- `IllegalArgumentException`/`IllegalStateException` 한글 메시지 정상화
- 운영자명 문자열(`SOS 운영자`) 복구

3. 잔여 점검
- `?[가-힣]`, `U+FFFD` 패턴 재검색으로 잔여 깨짐 없음 확인

### 검증
- `cd sosos && .\\mvnw.cmd -q -DskipTests compile` 성공

### 문서 동기화
- 추가: `docs/daily/learning/2026-04-30-korean-garbled-text-recovery-learning.md`

---

## 추가 작업 - 전역 깨진 한글 점검 및 재발 방지

### 메타 정보
- 오늘 목표: 프로젝트 전역 검수로 깨진 한글 여부를 확인하고 재발 방지 장치를 적용
- 완료 기준(DoD): 코드/설정 파일 기준 깨진 한글 패턴이 0건이고, UTF-8 강제/자동 점검 절차가 반영된다.

### 의사결정 1줄
- 단발성 수정보다 저장 인코딩 강제 + 자동 스캔 스크립트를 함께 넣어 반복 발생을 막는다.

### 작업 내용
1. 전역 스캔
- `frontend/src`, `sosos/src` 포함 전역 패턴 점검
- 코드 파일 기준 깨진 한글 패턴 미검출 확인

2. 재발 방지 설정
- 루트 `.editorconfig` 추가 (`charset=utf-8`, 줄바꿈/eol 규칙)
- `.vscode/settings.json`에 UTF-8 고정 설정 추가
  - `files.encoding = utf8`
  - `files.autoGuessEncoding = false`

3. 자동 점검 스크립트 추가
- `scripts/check-korean-garbled.ps1` 추가
- 검사 패턴:
  - `U+FFFD`
  - `?[가-힣]`
  - `U+F900~U+FAFF` 호환 한자 구간
- 문서 설명 예시 오탐 방지를 위해 코드/설정 파일 중심으로 검사

4. 체크리스트 반영
- `docs/daily/codex-preflight-checklist.md`에
  - `깨진 한글 패턴 점검 실행` 항목 추가

### 검증
- `cd . && .\\scripts\\check-korean-garbled.ps1` 성공
- `cd sosos && .\\mvnw.cmd -q -DskipTests compile` 성공

### 문서 동기화
- 추가: `docs/daily/learning/2026-04-30-korean-encoding-guard-learning.md`

---

## 추가 작업 - Java 핵심 코드 한글 주석 확장

### 메타 정보
- 오늘 목표: 서비스/컨트롤러/설정 계층의 중요한 Java 로직에 한글 주석을 보강
- 완료 기준(DoD): 핵심 흐름 주석 추가 후 한글 깨짐 없이 빌드 통과

### 의사결정 1줄
- 주석은 “코드가 뭘 하는지”보다 “왜 이 검증/차단/보정이 필요한지” 중심으로 최소 추가한다.

### 작업 내용
1. 주석 보강 대상
- 주석이 비어 있던 핵심 Java 파일 13개 선별
- 인증/권한 검증, 소유권 제한, 정렬 보정, 업로드 경로 보안 등 핵심 지점에 한글 주석 추가

2. 반영 파일
- `config/PasswordConfig.java`
- `controller/OrderController.java`
- `controller/SellerController.java`
- `controller/UserController.java`
- `controller/api/v1/DiscoveryTabApiController.java`
- `controller/api/v1/MainBannerApiController.java`
- `controller/api/v1/OrderApiController.java`
- `controller/api/v1/UserApiController.java`
- `controller/api/v1/WishlistApiController.java`
- `service/DiscoveryTabService.java`
- `service/MainBannerService.java`
- `service/storage/FileStorageService.java`
- `service/storage/LocalFileStorageService.java`

3. 품질 점검
- 서비스/컨트롤러/설정 계층에서 “주석 0개 파일” 재검사 결과 0건

### 검증
- `cd . && .\\scripts\\check-korean-garbled.ps1` 성공
- `cd sosos && .\\mvnw.cmd -q -DskipTests compile` 성공

### 문서 동기화
- 추가: `docs/daily/learning/2026-04-30-java-korean-comment-expansion-learning.md`

---

## 추가 작업 - 판매자 대시보드 대형 파일 분해 + 서버 재가동

### 목표
- `SellerDashboardPage.jsx` 대형 파일을 기능별 컴포넌트로 분리해 유지보수성을 높인다.
- 소비자/판매자 기능 영향 여부를 빌드/컴파일/기동 점검으로 확인한다.

### 의사결정 1줄
- 판매자 대시보드를 탭/다이얼로그 단위 컴포넌트로 분리해 페이지 파일을 250줄 이하로 축소한다.

### 변경 사항
1. 리팩터링
- `frontend/src/pages/SellerDashboardPage.jsx`를 220줄로 축소
- 신규 분리 파일 추가:
  - `frontend/src/pages/seller-dashboard/sellerDashboardUtils.js`
  - `frontend/src/pages/seller-dashboard/useSellerDashboardController.js`
  - `frontend/src/components/seller-dashboard/OverviewTabPanel.jsx`
  - `frontend/src/components/seller-dashboard/HomeBannerManager.jsx`
  - `frontend/src/components/seller-dashboard/DiscoveryTabManager.jsx`
  - `frontend/src/components/seller-dashboard/HomeTabPanel.jsx`
  - `frontend/src/components/seller-dashboard/ProductsTabPanel.jsx`
  - `frontend/src/components/seller-dashboard/OrdersTabPanel.jsx`
  - `frontend/src/components/seller-dashboard/InquiriesTabPanel.jsx`
  - `frontend/src/components/seller-dashboard/ProductDialog.jsx`

2. 문서
- 학습 문서 추가: `docs/daily/learning/2026-04-30-react-large-component-refactor-learning.md`
- 학습 인덱스 반영: `docs/daily/learning/README.md` (87개)

### 검증
- 프론트 빌드: `cd frontend && npm run build` 성공
- 백엔드 컴파일: `cd sosos && .\mvnw.cmd -q -DskipTests compile` 성공
- 깨진 한글 점검: `.\scripts\check-korean-garbled.ps1` 성공
- 서버 재가동: `.\scripts\start-renewal-dev.ps1 -SkipRemoteGuard` 성공
  - Backend: `http://localhost:8085`
  - Frontend: `http://localhost:3000`
- 접근성 확인:
  - `http://localhost:3000` -> 200
  - `http://localhost:3000/admin/dashboard` -> 200
  - `http://localhost:8085/api/v1/products` -> 200

---

## 추가 작업 - Home/MyPage 모듈 분해 및 폴더 재분류

### 목표
- `HomePage.jsx`, `MyPagePage.jsx`의 대형 단일 파일 구조를 분해해 유지보수성과 탐색성을 높인다.
- 파일 위치를 한눈에 찾을 수 있도록 기능 기준 폴더로 재배치한다.

### 의사결정 1줄
- 라우트 페이지는 조립자 역할만 남기고, 상태/이벤트는 `pages/<feature>/use*Controller`로 이동한다.

### 변경 사항
1. 홈 화면 분해
- 진입점 축소: `frontend/src/pages/HomePage.jsx` (112줄)
- 신규 분리:
  - `frontend/src/pages/home/homeConfig.js`
  - `frontend/src/pages/home/homeUtils.js`
  - `frontend/src/pages/home/homeSearchStorage.js`
  - `frontend/src/pages/home/useHomePageController.js`
  - `frontend/src/components/home/HomeHeroBanner.jsx`
  - `frontend/src/components/home/HomeFilterTabs.jsx`
  - `frontend/src/components/home/HomeSearchSummaryBar.jsx`
  - `frontend/src/components/home/HomeSearchDialog.jsx`

2. 마이페이지 분해
- 진입점 축소: `frontend/src/pages/MyPagePage.jsx` (176줄)
- 신규 분리:
  - `frontend/src/pages/mypage/myPageConfig.js`
  - `frontend/src/pages/mypage/myPageUtils.js`
  - `frontend/src/pages/mypage/useMyPageController.js`
  - `frontend/src/components/mypage/MyPageHeaderCard.jsx`
  - `frontend/src/components/mypage/MyPageSectionCard.jsx`
  - `frontend/src/components/mypage/MyPageOrdersSection.jsx`
  - `frontend/src/components/mypage/MyPageBenefitsSection.jsx`
  - `frontend/src/components/mypage/MyPageInterestSection.jsx`
  - `frontend/src/components/mypage/MyPageReviewsSection.jsx`
  - `frontend/src/components/mypage/MyPageShippingSection.jsx`
  - `frontend/src/components/mypage/MyPageAccountSection.jsx`

3. 구조 안내 문서 추가
- `frontend/src/FEATURE_STRUCTURE.md` 추가
  - 홈/마이페이지 관련 파일 위치를 기능 단위로 정리

### 검증
- 프론트 린트: `cd frontend && npm run lint` 성공
- 프론트 빌드: `cd frontend && npm run build` 성공
- 백엔드 컴파일: `cd sosos && .\mvnw.cmd -q -DskipTests compile` 성공
- 깨진 한글 점검: `.\scripts\check-korean-garbled.ps1` 성공

---

## 추가 작업 - 컨트롤러 반환 도메인 그룹화(유지보수 2차)

### 목표
- `useXxxController`의 평면 반환 구조를 도메인 그룹으로 전환해 페이지 가독성과 유지보수성을 개선한다.

### 의사결정 1줄
- 페이지는 조립자 역할만 수행하고, 컨트롤러는 `auth/ui/data/actions` 같은 그룹 객체로 반환한다.

### 변경 사항
1. MyPage 그룹화
- `frontend/src/pages/mypage/useMyPageController.js`
  - 반환 구조를 `auth/layout/orderView/interestView/reviewView/accountView/actions`로 재구성
- `frontend/src/pages/MyPagePage.jsx`
  - 긴 destructuring 제거 후 그룹 기반 참조로 변경

2. Home 그룹화
- `frontend/src/pages/home/useHomePageController.js`
  - 반환 구조를 `hero/filters/listing/searchDialog/wishlist/authGuard/feedback`로 재구성
- `frontend/src/pages/HomePage.jsx`
  - 그룹 기반 참조로 변경

3. SellerDashboard 그룹화
- `frontend/src/pages/seller-dashboard/useSellerDashboardController.js`
  - 반환 구조를 `ui/data/metrics/orderView/inquiryView/homeManageView/productDialogView/actions`로 재구성
- `frontend/src/pages/SellerDashboardPage.jsx`
  - 그룹 기반 참조로 변경

4. 공통 유지보수 가드
- `frontend/eslint.config.js`
  - `max-lines`, `max-lines-per-function` 경고 규칙 추가(에러 차단 대신 경고 모니터링)
- `frontend/src/FEATURE_STRUCTURE.md`
  - 판매자 대시보드 구조 및 컨트롤러 반환 규칙 추가

5. 학습 문서
- `docs/daily/learning/2026-04-30-react-controller-domain-grouping.md` 추가
- `docs/daily/learning/README.md` 인덱스 반영(89개)

### 검증
- 프론트 린트: `cd frontend && npm run lint` 성공 (에러 0, 경고 다수)
- 프론트 빌드: `cd frontend && npm run build` 성공
- 백엔드 컴파일: `cd sosos && .\mvnw.cmd -q -DskipTests compile` 성공
- 깨진 한글 점검: `.\scripts\check-korean-garbled.ps1` 성공
- 스모크:
  - `http://localhost:3000` -> 200
  - `http://localhost:3000/mypage` -> 200
  - `http://localhost:3000/admin/dashboard` -> 200
  - `http://localhost:8085/api/v1/products` -> 200

---

## 추가 작업 - 모의 결제창 적용 + 결제 보안 가드

### 목표
- 실제 과금 없는 결제 UX를 추가하고, 민감정보 처리 범위를 최소화한다.

### 의사결정 1줄
- 주문 API는 기존 그대로 유지하고, 프론트에서 모의 결제 승인 후에만 주문 API를 호출한다.

### 변경 사항
1. 공통 결제 다이얼로그 추가
- `frontend/src/components/payment/MockPaymentDialog.jsx` 신규
  - 테스트 결제 전용 UI(실청구 없음 안내)
  - 카드번호/유효기간/CVC 형식 검증
  - 테스트 카드 번호만 허용(안전장치)
  - 카드 원문 서버 전송 없음(주문 API에는 결제 민감정보 미전달)
  - 다이얼로그 종료 시 민감 입력값 초기화

2. 상품 상세 바로주문 연결
- `frontend/src/pages/ProductDetailPage.jsx`
  - 기존 `바로 주문` 클릭 시 즉시 주문 API 호출하던 흐름을
  - `모의 결제 승인 -> 주문 API 호출` 흐름으로 변경

3. 장바구니 주문 연결
- `frontend/src/pages/CartPage.jsx`
  - 기존 `장바구니 주문하기` 즉시 호출을
  - `모의 결제 승인 -> 주문 API 호출`로 변경

4. 학습 문서
- `docs/daily/learning/2026-04-30-mock-payment-dialog-security-learning.md` 추가
- 학습 인덱스 갱신 (`docs/daily/learning/README.md`, 90개)

### 검증
- 프론트 린트: `cd frontend && npm run lint` 성공 (에러 0, 경고 다수)
- 프론트 빌드: `cd frontend && npm run build` 성공
- 백엔드 컴파일: `cd sosos && .\mvnw.cmd -q -DskipTests compile` 성공
- 깨진 한글 점검: `.\scripts/check-korean-garbled.ps1` 성공

---

## 추가 작업 - AI 멀티턴 문맥 추천 품질 패치

### 목표
- 2~3턴 이후 답변 품질 저하(짧아짐/문맥 단절)를 줄이고, 상하의 조합 같은 후속 질의를 자연스럽게 처리한다.

### 의사결정 1줄
- 단일 메시지 처리 구조를 유지하면 후속 질의 품질이 흔들려서, `history 전송 + 서버 문맥 해석 + 응답 품질 게이트`를 함께 적용한다.

### 변경 사항
1. 프론트 히스토리 전송
- `frontend/src/components/AiStylistWidget.jsx`
- `frontend/src/services/aiStylistApi.js`
- 최근 대화(최대 8개)를 요청 payload `history`에 포함

2. 백엔드 멀티턴 처리
- `sosos/src/main/java/com/prosos/sosos/dto/AiStylistChatRequest.java`
- `sosos/src/main/java/com/prosos/sosos/controller/api/v1/AiStylistApiController.java`
- `sosos/src/main/java/com/prosos/sosos/service/AiStylistService.java`
- `chat(message, history)` 시그니처 추가
- 최근 사용자 메시지 기반 문맥 합성 후 의도/토큰 파싱
- 요청 카테고리 누락 시 fallback 후보 보강
- Gemini 응답 품질 게이트(길이/형식/상품명 언급) 추가

3. 테스트 보강
- `sosos/src/test/java/com/prosos/sosos/controller/api/v1/AiStylistApiControllerTest.java`
- `sosos/src/test/java/com/prosos/sosos/service/AiStylistServiceTest.java`
- 멀티턴 후속 질의 시나리오 추가 및 시그니처 갱신

### 검증
- 백엔드 테스트: `cd sosos && .\\mvnw.cmd -q "-Dtest=AiStylistServiceTest,AiStylistApiControllerTest" test` 성공
- 프론트 린트: `cd frontend && npm run lint` 성공(에러 0, 경고만 존재)
- 프론트 빌드: `cd frontend && npm run build` 성공
- 깨진 한글 점검: `powershell -ExecutionPolicy Bypass -File scripts/check-korean-garbled.ps1` 성공

### 문서 동기화
- 추가: `docs/daily/learning/2026-04-30-ai-multiturn-natural-recommendation-learning.md`
- 수정: `docs/daily/learning/README.md`

---

## 추가 작업 - 토스 테스트 키 결제창 + 서버 승인 연동

### 목표
- 테스트 키로 실제 토스 결제창까지 연결하고, 시크릿 키는 서버 환경변수로만 처리한다.

### 의사결정 1줄
- 프론트는 결제창 호출만 담당하고, 최종 승인(confirm)은 백엔드에서 수행한다.

### 변경 사항
1. 백엔드 승인 API 추가
- `sosos/src/main/java/com/prosos/sosos/controller/api/v1/PaymentApiController.java`
- `sosos/src/main/java/com/prosos/sosos/service/TossPaymentService.java`
- `sosos/src/main/java/com/prosos/sosos/dto/TossPaymentConfirmRequest.java`
- `sosos/src/main/java/com/prosos/sosos/dto/TossPaymentConfirmResult.java`
- 설정:
  - `sosos/src/main/resources/application-dev.properties`
  - `sosos/src/main/resources/application-prod.properties`
  - 키: `TOSS_ENABLED`, `TOSS_SECRET_KEY`, `TOSS_CONFIRM_URL`

2. 프론트 결제 흐름 구성
- `frontend/src/pages/CheckoutPage.jsx`
  - 주문서에서 토스 결제창 호출
- `frontend/src/pages/CheckoutTossSuccessPage.jsx`
  - 성공 URL -> 백엔드 승인 -> 기존 주문 API 호출
- `frontend/src/pages/CheckoutTossFailPage.jsx`
  - 실패 안내 + 주문서 복귀
- `frontend/src/utils/tossPaymentsSdk.js`
  - 토스 SDK 로더/결제창 호출 유틸
- `frontend/src/pages/checkout/checkoutStorage.js`
  - 결제 전 주문 컨텍스트 임시 저장
- `frontend/src/services/paymentApi.js`
  - `/api/v1/payments/toss/confirm` 호출
- 라우트:
  - `frontend/src/App.jsx`
  - `/checkout/toss/success`, `/checkout/toss/fail` 추가

3. 주문 진입점 유지보수
- `frontend/src/pages/ProductDetailPage.jsx`
  - `주문` -> `/checkout` 이동
- `frontend/src/pages/CartPage.jsx`
  - `장바구니 주문하기` -> `/checkout` 이동

4. 환경변수 문서/예시 반영
- `frontend/.env.example`
- `frontend/README.md`
- `sosos/.env.example`
- `sosos/docs/deployment/env-standard.md`

5. 학습 문서
- `docs/daily/learning/2026-04-30-toss-test-key-checkout-flow-learning.md`
- 인덱스 반영: `docs/daily/learning/README.md` (91개)

### 검증
- 프론트 린트: `cd frontend && npm run lint` 성공 (에러 0, 경고 다수)
- 프론트 빌드: `cd frontend && npm run build` 성공
- 백엔드 컴파일: `cd sosos && .\mvnw.cmd -q -DskipTests compile` 성공
- 깨진 한글 점검: `.\scripts\check-korean-garbled.ps1` 성공
- 스모크:
  - `http://localhost:3000` -> 200
  - `http://localhost:3000/checkout` -> 200
  - `http://localhost:3000/checkout/toss/success` -> 200
  - `http://localhost:8085/api/v1/products` -> 200
  - `POST /api/v1/payments/toss/confirm` (비로그인) -> 401
- 스모크:
  - `http://localhost:3000` -> 200
  - `http://localhost:3000/cart` -> 200
  - `http://localhost:3000/products/1` -> 200
  - `http://localhost:3000/mypage` -> 200
  - `http://localhost:3000/admin/dashboard` -> 200
  - `http://localhost:8085/api/v1/products` -> 200

---

## 추가 작업 - 주문서 페이지 선행 후 결제창 오픈

### 목표
- 결제 UX를 실제 쇼핑몰 흐름처럼 `주문서 -> 결제창` 순서로 전환한다.

### 의사결정 1줄
- 주문 API 자체는 변경하지 않고, 상세/장바구니에서 `checkout` 페이지를 거쳐 최종 결제 승인 시점에만 주문 API를 호출한다.

### 변경 사항
1. 주문서 페이지 신규 추가
- `frontend/src/pages/CheckoutPage.jsx`
  - 배송지(수령인/연락처/주소/요청사항)
  - 주문상품 목록
  - 결제수단 선택
  - 우측 결제금액 요약 카드
  - 최종 버튼 클릭 시 모의 결제창 오픈

2. 라우팅 연결
- `frontend/src/App.jsx`
  - 사용자 전용 라우트에 `/checkout` 추가

3. 기존 페이지 흐름 변경(다른 기능 수정 없음)
- `frontend/src/pages/ProductDetailPage.jsx`
  - `주문` 버튼: 즉시 주문 API 호출 -> `/checkout` 이동으로 변경
- `frontend/src/pages/CartPage.jsx`
  - `장바구니 주문하기`: 즉시 주문 API 호출 -> `/checkout` 이동으로 변경

4. 보안 유지
- 카드 원문/CVC 서버 전송 없음
- 모의 결제창 닫힘 시 민감 입력 초기화
- 테스트 카드 패턴만 허용

### 검증
- 프론트 린트: `cd frontend && npm run lint` 성공 (에러 0, 경고 다수)
- 프론트 빌드: `cd frontend && npm run build` 성공
- 백엔드 컴파일: `cd sosos && .\mvnw.cmd -q -DskipTests compile` 성공
- 깨진 한글 점검: `.\scripts/check-korean-garbled.ps1` 성공
