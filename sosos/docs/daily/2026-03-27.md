# Daily Log - 2026-03-27

## 메타 정보
- 프로젝트: SOS 리뉴얼
- 우선순위: P2
- 오늘 목표: 알림 데이터 모델/API 설계 확정 및 실동작 검증
- 완료 기준(DoD): 알림 DB 저장, 목록/요약/읽음 API, 트리거 생성, 프론트 연동, 빌드 검증 통과

## 의사결정 1줄
- 알림은 로컬 파생형이 아니라 DB 저장형으로 전환한다. 이유: 계정/기기/세션이 바뀌어도 이력이 유지되어야 하기 때문.

## 사전점검 체크리스트
- [x] 구현 전 학습 문서 작성 (`무엇/왜/언제/어떻게`)
- [x] 작업 파일 경로/완료 기준 사전 명시
- [x] 프론트/백엔드 검증 명령 실행 계획 수립

## 오늘 작업
1. 알림 백엔드 모델/API 확정
- 신규 파일
  - `src/main/java/com/prosos/sosos/model/Notification.java`
  - `src/main/java/com/prosos/sosos/repository/NotificationRepository.java`
  - `src/main/java/com/prosos/sosos/dto/NotificationDto.java`
  - `src/main/java/com/prosos/sosos/service/NotificationService.java`
  - `src/main/java/com/prosos/sosos/controller/api/v1/NotificationApiController.java`
- API
  - `GET /api/v1/notifications`
  - `GET /api/v1/notifications/summary`
  - `PUT /api/v1/notifications/{id}/read`
  - `PUT /api/v1/notifications/read-all`

2. 알림 트리거 연동
- `SellerService`
  - 주문 상태 변경 시 알림 생성
  - 상품 수정 시 재입고/할인 감지 후 찜 사용자 대상 알림 생성
  - 문의 답변 등록/수정 시 알림 생성
- `InquiryController`
  - 관리자 답변 등록/수정 시 알림 생성

3. 알림 프론트 전환
- `frontend/src/services/notificationApi.js`
  - 서버 API 호출 기반으로 교체
- `frontend/src/pages/NotificationsPage.jsx`
  - 목록/요약 조회, 개별 읽음, 전체 읽음, 유형 필터 반영
  - 401 세션 만료 시 로그인 이동 처리 유지

4. DB 스키마 반영
- 신규 마이그레이션: `db/migrations/2026-03-27-notification-schema.sql`
- 로컬 시드 보강: `db/seed/mydb.sql`에 `notifications` 테이블 추가

## 검증 결과
- 백엔드 컴파일: 성공
  - `cd sosos && .\mvnw.cmd -DskipTests compile`
- 프론트 린트: 성공
  - `cd frontend && npm run lint`
- 프론트 빌드: 성공
  - `cd frontend && npm run build`
  - 참고: 번들 경고(500KB+)는 기존과 동일, 별도 대응 필요

## 이슈/해결
- 이슈: 일부 파일에 UTF-8 BOM(`\ufeff`)이 포함되어 자바 컴파일 실패
- 해결: `NotificationService`, `NotificationApiController`, `SellerService` BOM 제거 후 재컴파일

## 다음 작업 후보
1. 번들 크기 경고(500KB+) 대응 전략 수립
2. 알림 운영 고도화(헤더 배지, 관리자 공지형 알림) 범위 결정
3. 마이페이지 잔여 섹션 실제 API 연동 확대

## 추가 작업 (마이페이지 리뷰 + 알림 배지)
1. 마이페이지 리뷰 실연동
- 백엔드 신규
  - `src/main/java/com/prosos/sosos/model/ProductReview.java`
  - `src/main/java/com/prosos/sosos/repository/ProductReviewRepository.java`
  - `src/main/java/com/prosos/sosos/dto/ProductReviewDto.java`
  - `src/main/java/com/prosos/sosos/dto/ProductReviewCreateRequest.java`
  - `src/main/java/com/prosos/sosos/service/ProductReviewService.java`
  - `src/main/java/com/prosos/sosos/controller/api/v1/ProductReviewApiController.java`
- API
  - `GET /api/v1/reviews/me` (내 리뷰 조회)
  - `POST /api/v1/reviews/me` (주문 기반 리뷰 등록)
- 규칙
  - 본인 주문만 리뷰 작성 가능
  - 주문당 1개 리뷰 제한
  - 취소 주문(CANCELLED) 리뷰 작성 제한

2. 마이페이지 프론트 연결
- 신규: `frontend/src/services/reviewApi.js`
- 수정: `frontend/src/pages/MyPagePage.jsx`
  - 리뷰 작성 폼(주문 선택/평점/내용)
  - 내 리뷰 목록/요약(건수, 평균 평점)
  - 문의 블록과 함께 섹션 4에서 실데이터 렌더링

3. 알림 배지 실시간 반영
- 수정: `frontend/src/components/AppLayout.jsx`
  - `/api/v1/notifications/summary` 기반 unread 배지 표시
  - 30초 폴링 + 이벤트(`sosos:notification-summary-refresh`) 동기화
- 수정: `frontend/src/pages/NotificationsPage.jsx`
  - 30초 무소음 재조회(silent polling)
  - 읽음/전체읽음 후 헤더 배지 동기화 이벤트 발행

4. DB 반영
- 신규 마이그레이션: `db/migrations/2026-03-27-product-review-schema.sql`
- 시드 보강: `db/seed/mydb.sql`에 `product_reviews` 테이블 추가
- 로컬 적용 결과: `product review schema upgrade done`

## 추가 검증 결과
- 리뷰/알림 실사용 API 시나리오: 성공
  - 사용자 가입/로그인 -> 상품 구매 -> 리뷰 등록/조회 성공
  - 동일 주문 중복 리뷰 등록 차단(`HTTP 400`) 확인
  - 주문 상태 변경 알림 생성 -> 요약 unread 증가 확인
  - 개별 읽음 처리 -> unread 감소(0) 확인

## 추가 작업 (이미지/파일 저장 전략 + 문서 정리)
1. 이미지/파일 저장 전략 코드 반영
- 스토리지 추상화 도입
  - `FileStorageService`
  - `LocalFileStorageService` (`app.storage.type=local`)
  - `S3FileStorageService` (`app.storage.type=s3`)
- 업로드 로직 교체
  - `SellerService`, `MainBannerService`의 직접 파일 저장 로직 제거
  - 공통 스토리지 호출 방식으로 변경
- 조회 정책 정리
  - `WebConfig`에서 `local`일 때만 `/images/**` 리소스 매핑
- 설정 분리
  - `application-dev.properties`: `APP_STORAGE_TYPE` 기본 `local`
  - `application-prod.properties`: `APP_STORAGE_TYPE` 기본 `s3`, S3 설정키 분리

2. 로컬 S3 모드 스모크 검증
- `APP_STORAGE_TYPE=s3`로 로컬 서버 기동 확인
- 배너 업로드 API 호출 경로 확인
- AWS 자격증명 미설정 상태에서
  - `Unable to load credentials from any of the providers...` 오류 확인
  - 결론: S3 코드 경로 정상, 실제 자격증명 기반 업로드 검증만 남음

3. 문서 동기화
- `README.md`, `master-renewal-roadmap.md`, `work-summary.md`, `today-action-board.md` 최신화
- `daily/learning/README.md` 누락 학습 문서 링크 보강
- 다음 단계 문서 기준 확정
  - 배포(AWS) 전: 로컬/운영 리허설 기준 문서화
  - 배포(AWS) 시: EC2+IAM Role 검증 로그 추가

## 추가 작업 (MD 전수 점검)
1. 전체 MD 파일 전수 확인
- 대상: 저장소 내 `*.md` 86개
- 결과: 파일별 분류/상태 점검 완료 (`docs/md-audit-2026-03-27.md`)

2. 자동 검증
- UTF-8 strict 디코딩 검증: 이상 0건
- replacement character 검증: 이상 0건
- 상대 링크 존재 검증: 이상 0건

3. 정리 기준
- 현재 상태 문서: `README.md`, `docs/master-renewal-roadmap.md`, `docs/work-summary.md`, `docs/daily/today-action-board.md` 우선 동기화
- 이력 문서: 본문 변경 최소화, 인덱스/링크만 점검
