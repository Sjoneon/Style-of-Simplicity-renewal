# TIL - 2026-02-19

## 오늘 잘한 점
- 기존 컨트롤러를 건드리지 않고 `/api/v1/*`를 추가해 하위 호환성을 유지했다.
- 응답 포맷을 `success/data/message`로 맞춰 프런트 처리 규칙을 단순화했다.

## 개선할 점
- 현재 `/api/*` 레거시 컨트롤러와 `/api/v1/*`가 병행되므로 추후 라우트 이관 계획이 필요하다.

## 오늘 배운 점
- 경계 분리 작업은 "URL 추가 + 응답 표준화 + 세션 규칙 유지"를 함께 맞춰야 실제 전환 비용이 줄어든다.

## 다음 액션
- `/api/v1/*` 경로를 기준으로 프론트 API 클라이언트 초안을 붙이고 회귀 테스트 목록을 만든다.
